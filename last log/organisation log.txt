look in folder "last log" and thoroughly read. there are logs inside. find the instructions (in its own folder) about structure, process of "tearing apart", storing and preserving every useful, interesting, promising, etc piece of info. first collect them into one as one continuous thing. and then dissect into parts that would go into apropriate folders.

also, the result of v9 - it works, feats are present, don't reset after soft or hard quiting. they go up if i do the right thing. one thing im not sure about - the feat icon remains bronze when loading earlier save (when i didn't complete conditions). or when restarting the compaign. so the message "world heard about your deeds(or something, don't remember clearly)" doesn't show, since bronze feat is already "achieved" (probably cache files applies to every save, unless deleted)

Let me explore the repository first.

Let's look at "last log" folder and the 00_START_HERE folder for instructions.

Let me read the instructions folder (00_START_HERE), especially PROMPT_organize_research_log_v6.md (latest version) and the README/PLAN, ORGANIZATION_HISTORY, UPLOAD_GUIDE, STATUS.

Also read the "last log" folder files. Let me start.

Now let's read v4 (the raw-log dissection A-F rules), v5, and STATUS.md, ORGANIZATION_HISTORY, INGEST_PLAN_DUMP, INGEST_REPORT_DUMP.

Now let's read the STATUS.md, CASES_AND_FINDINGS.md, and the V9_COLD_LOAD_FEATS_FIX.md (relevant to user's second issue about feat icon remaining bronze when loading earlier save / restart). Also NEXT_SESSION_HANDOFF_2026-08-30.md.

Also the "last log" folder content. Let's start scanning the logs.

The task:

    Read "last log" folder thoroughly (logs inside).
    Find the instructions (in its own folder = 00_START_HERE) about structure, process of "tearing apart", storing and preserving every useful, interesting, promising piece of info.
    First collect them into one as one continuous thing (i.e., a consolidated instruction document — probably PROMPT_organize_research_log_v7.md or a unified "MASTER_INGEST_RULES.md").
    Then dissect into parts that would go into appropriate folders (i.e., apply those instructions to the "last log" folder content: extract net-new facts, place artifacts into 01_research_archives, 03_analysis, 04, 05, 07, 11, 12, 13, etc., update living docs).
    Also address the v9 result: works, feats present, don't reset after soft/hard quit, they go up when doing the right thing. One uncertainty: feat icon remains bronze when loading earlier save (before completing conditions) or when restarting the campaign. The message "world heard about your deeds" doesn't show because bronze feat is already "achieved" — probably cache file applies to every save unless deleted.

So I need to investigate the feat cache issue — the q847rsja8ndx feat cache. Let me look at 13_save_and_cache and the analysis docs about feat cache and the "world heard" message.

Let me read a lot. Start with STATUS.md and CASES_AND_FINDINGS.md, V9_COLD_LOAD_FEATS_FIX.md, and NEXT_SESSION_HANDOFF_2026-08-30.md.

Now, the user says: v9 works! Feats present, don't reset after soft/hard quit, they go up if doing the right thing. One uncertainty: feat icon remains bronze when loading an earlier save (when conditions weren't completed), or when restarting the campaign. So the "world heard about your deeds" message doesn't show since bronze feat is already "achieved" (probably cache file applies to every save unless deleted).

So I need to:

    Read "last log" folder thoroughly (all 11 files) — big files (~1.9 MB total). I need to read them strategically but thoroughly. Let me look at the beginnings and structure of each.
    Consolidate the instructions into one continuous document (v7 prompt? or "MASTER_ORGANIZING_INSTRUCTIONS.md"). The user says "find the instructions (in its own folder) about structure, process of 'tearing apart', storing and preserving every useful, interesting, promising, etc piece of info. first collect them into one as one continuous thing." So: create a single consolidated instructions file (probably 00_START_HERE/MASTER_INGEST_RULES.md or PROMPT_organize_research_log_v7.md combining v4+v5+v6 rules continuously).
    Then dissect "last log" content into parts that go into appropriate folders: e.g., Part 5 archive in 01_research_archives, net-new extracts in 03_analysis, V9 runtime results in 03_analysis, patch/trace files in 05_patches_and_scripts, git patch in 11_git_patch, etc. Update living docs (STATUS, CASES, MASTER, PLAN, README, CONTRADICTIONS, INDEX).
    Address the feat cache issue: the icon remains bronze on an earlier save / new campaign because the local feat cache (q847rsja8ndx) is account/profile-wide, not per-save. So the "world heard about your deeds" popup only fires on the transition from not-achieved to achieved, which won't happen again because the cache already stores the peak tier.

I should verify claims: check the feat cache file in 13_save_and_cache, and look at the raw logs for the cache key/format and the event/message key for "world heard" (maybe an event like bronze_achievement / notification "world_heard_of_your_deeds"). Let me grep the game data / strings for relevant loc keys.

Let me start reading the "last log" folder files. Let me check line counts and structure.

The "last log (for now).txt" is a raw chat export of the previous session (which produced V9). The user now wants me to:

    Read the whole "last log" folder.
    Consolidate instructions into one continuous doc.
    Dissect the folder's content into appropriate folders (per those instructions).
    Report the V9 result and address the feat cache / bronze icon question.

Let me thoroughly read the logs. I'll need to work through ~1.9 MB of text. Let me approach systematically: for each file, read fully (in chunks) or at least grep for key facts. The instructions demand preserving verbatim hashes, offsets, addresses, user runtime reports, etc.

Note: "first raw log.txt" and "raw chat.txt" have identical sizes (311,875 B). Let me check if they're byte-identical — that's part of the dedup rule (SHA-256 dedup).

Let's compute SHA-256 of all files in "last log".

Good — all 11 files in "last log" are unique (no duplicates), though "first raw log.txt" and "raw chat.txt" have the same size but different hashes (interesting — check if they overlap in content).

Now let me read all the files. Start with the smaller ones fully, then work through the larger ones. Let me read:

    MJ_V8_CLEAN_TRACE.txt (48 lines)
    RUN_MJ_V8_CLEAN_TRACE.bat (3KB)
    one more raw log.txt (426 lines)
    first raw log.txt (497 lines)
    x64dbg logs.txt (504 lines)
    another other raw log.txt (1142 lines)
    another raw log.txt (3743 lines)
    first first raw log.txt (4358 lines)
    raw chat.txt (4346 lines)
    last log (for now).txt (6373 lines)
    the .patch

Let me read them.

Excellent — this "one more raw log.txt" is the V8 test session: user applied V8, feats 0 in game AND main menu; reverted; cache q847rsja8ndx content pasted (with conquerer_from_bribir=1, established=4, category=-1991027533); the BAT flicker root cause (cmd.exe parsing parentheses inside if block); PowerShell direct works.

Let me see the truncated middle part of that file — I need the full cache dump content. Let me view the middle of the file.

Then read the rest of the files. Let me be efficient: I'll read each file with pagination. Let me use a script to output sections.

Let me now read the truncated portion of "one more raw log.txt" (lines ~200-330).

Now I have the full cache file content: key=-2128831035, id=-2128831035, user_id=1179784490, ... conquerer_from_bribir=1, established=4, ... category=-1991027533.

Wait — earlier the STATUS said preflight user_id=84696387 stable. Here the cache says user_id=1179784490. That's a potential contradiction to record in CONTRADICTIONS.md! Let me verify by grepping the repo for 84696387 and 1179784490.

Now let's read "first raw log.txt" (497 lines).

"first raw log.txt" is the session log where the patch 01a044b2 was applied and PR'd. Let me read the rest.

Note: the feat cache in the repo (13_save_and_cache/q847rsja8ndx.txt) has key=-2128831035, id=-2128831035, and user_id? Let me check the file in repo. The one in "one more raw log.txt" has user_id=1179784490 — a NEW value. That is net-new info (a third user_id value). Also category=-1991027533.

Let me check 13_save_and_cache contents.

Now, "first raw log.txt" = the session that landed the V8 patch (PR #13, merge commit 96ba84b, branch arena/01a04980). Net-new facts: hash chain reproduction stock→V5→V6→V7→V8, PR #13, merge commit 96ba84b, branch id.

Let me now read the remaining bigger files. Next: "x64dbg logs.txt" (504 lines).

The x64dbg log has a lot of register-dump noise. Let me extract the meaningful lines: [MJ] lines, "Application debug", "Total loadtime", breakpoint set lines, and the debug strings.

This confirms the V8 trace facts (already archived in V9_COLD_LOAD_FEATS_FIX.md). Let me look for other interesting details in x64dbg logs — e.g. missing text keys, error strings, Steam API fail, and lines 240-280 (breakpoint set messages), and anything around 388-399 (the "Total loadtime" and the message that preceded it).

Let me grep for "Application debug" lines and other strings in the x64dbg log, plus lines 240-280.

Now let me look at other unique content in x64dbg logs: "missing text" or other debug strings. Let me grep for "Строка отладки" (debug string) non-"Application debug" ones.

The x64dbg log is fully captured by the existing V9 doc. Good — mostly already archived; net-new: the exact breakpoints set VAs (base 0x00007FF7B6EE0C00? let's compute: DAILY_GATE rip=00007FF7B7546146 with module offset 0x666146 → base = 0x7FF7B7546146 - 0x666146 = 0x7FF7B6EE0000. So ASLR base 0x00007FF7B6EE0000 in this session — a fact worth noting (CK2game.exe loaded at 0x7FF7B6EE0000). Also "Kernel Debug character.cpp:1624 CCharacter::~CCharacter -> Deleting playerdata for char #470001", fixedwindow warnings for subwindow ruler / start_notification in window 2 open, missing text keys list, loadtime 779.015 s, dynamic titles created at 1278.1.1 (b_dyn_2606630 etc.).

Let me check if the archive already has these. Let me grep for "nonexistant subwindow" and "779.015" in the repo.

Now, next files: "another other raw log.txt" (1142 lines), "another raw log.txt" (3743), "first first raw log.txt" (4358), "raw chat.txt" (4346), "last log (for now).txt" (6373). Let me read them. Start with "another other raw log.txt" fully.

This is getting meta. The "another other raw log.txt" is a session log that reads "raw chat.txt" (which is the parent session log ending with a rejected PR request). Let me continue reading "another other raw log.txt" and then examine "raw chat.txt" and "first first raw log.txt" (probably the same as raw chat? sizes are 311,875 for both raw chat.txt and first first raw log.txt? No: first first raw log.txt = 312,285, raw chat.txt = 311,875. Similar but different).

Let me continue reading "another other raw log.txt" (lines 150-1142).

Good. This is the session that wrote NEXT_SESSION_HANDOFF_2026-08-29.md. Let me continue reading "another other raw log.txt" (520-1142).

Good: "another other raw log.txt" = session arena/01a049a4, which wrote NEXT_SESSION_HANDOFF_2026-08-29.md, PR #14, merge commit 910234875decd988ce55ed95e2401ce0f8c1b02a, commit ab07419. It also contains an important section listing key addresses (section 8 in the handoff) and the truncated middle part. Let me look at the truncated middle region (around lines 700-900) for the handoff structure and the verified facts.

Actually, let me grep that file for specific verbatim facts I may need.

Very useful. Net-new facts from this file:

    Linux symbol addresses for the feat machinery (CFeatProgressStorage: CacheProgress 0xf460bc, ReadCachedProgress 0xf46958, ReadProgressFromKeyValueStorage 0xf4731a, WipeFeats 0xf45f8a, SetNewProgress 0xf47220, SetCachedProgressIfHigher 0xf47534, Update 0xf46722; CRulerFeatTracker: ReloadFeatsDatabase 0xff7f7a, UpdateFeatProgress 0xff78a6, GetFeatLevel 0xff7c68, RecalcScore 0xff7cc2, SetFeatProgress 0xff7dfe, IsActiveForPlaythrough 0xff6d46, CalcShouldTrackFeatProgress 0xff6e38/0xff6e48 discrepancy).
    gameState+0x598 = special_event (Windows), Linux +0x570; tracker vectors +0x108 current, +0x120 cached.
    Payload: fc6ec025b782c811636a0efb65a7b3f192f09fffd0ff6ca8051ef8bc6113db4e, 101,949 bytes, 11 rulers, 33 challenges, event_time_end=1893499200 = 2030-01-01.
    V5 hash 29556549…
    PR #14, commit ab07419, merge commit 910234875decd988ce55ed95e2401ce0f8c1b02a.
    Contradiction: CalcShouldTrackFeatProgress lin 0xff6e38 vs 0xff6e48; win 0x1407b8450 (function start) vs 0x1407b8464 (call site).

I should verify some of these against the repo (e.g., check if the Linux addresses are already in the archive). Let me check NEXT_SESSION_HANDOFF_2026-08-29.md content (already in repo) — it likely includes those. And check whether the archive contains CFeatProgressStorage addresses.

Now let's read the remaining files: "another raw log.txt" (3743 lines), "first first raw log.txt" (4358), "raw chat.txt" (4346), "last log (for now).txt" (6373). These are large. Let me sample them: read the beginning and end, and grep for the user's messages (short lines that are user turns).

Let me first check "raw chat.txt" vs "first first raw log.txt" — similar size. Compare with diff to see how they differ.

"raw chat.txt" and "first first raw log.txt" are near-duplicates: the same session (arena/01a044b2 — the V8 session). "first first raw log.txt" is a longer export (4358 lines) including the "do", "work?", "what's going on?" trailing user messages and the branch footer. So "first first raw log.txt" is the fuller export of the same session; "raw chat.txt" is a slightly truncated export of the same.

Let me diff them to see what content differs (net-new in one vs other).

Confirmed: raw chat.txt is a strict prefix-subset of first first raw log.txt (the latter has 13 extra lines at the end). They are NOT byte-identical, but content-wise first first raw log.txt supersedes raw chat.txt (near-duplicate export of session arena/01a044b2).

Now the big one: "last log (for now).txt" (6373 lines) = the session that produced V9 (session arena/01a053d9? Actually current branch is arena/01a053d9... hmm, this session's branch is arena/01a053d9-ck2-monarch-s-journey-restorat. The "last log (for now).txt" begins with "look in folder 'last log' and thoroughly read. latest result should be in x64log (but probably incomplete again)" — that's the V9 session. Let me read it thoroughly.

Also "another raw log.txt" (3743 lines) — need to identify which session.

Let me look at heads/tails of both.

"another raw log.txt" = session arena/01a04d46, which created the MJ_V8_CLEAN_TRACE + RUN_MJ_V8_CLEAN_TRACE.bat and PR #15 (squash merge commit f29287217b300be83a0c6334ccddc9a780bd5092).

Note: PR #15 was merged with SQUASH, whereas PR #13/#14 with merge commit. Interesting: conventions drifted. Worth noting in a doc.

Now let me read "another raw log.txt" fully — 3743 lines. I'll read in chunks, focusing on user messages and technical content. Let me extract the user turns first (short lines without typical AI formatting). Actually, easier: read in chunks of ~500 lines but that's a lot of output. Let me be strategic: grep for key technical markers (offsets, hashes, [MJ], addresses, "cache", "feat") and read those regions.

Let me first get a map of the file: find lines that look like user turns (short, lowercase, no leading spaces, not part of code).

The first ~426 lines of "another raw log.txt" appear to be the embedded paste of the "one more raw log.txt" session (the handoff-based session). Indeed "one more raw log.txt" is a subset pasted inside "another raw log.txt" as the user's context ("read this thoroughly, also folder with latest logs..."). Let me verify: compare "one more raw log.txt" with lines 8-426 of "another raw log.txt".

Then the rest of "another raw log.txt" (from ~line 430) is the new session arena/01a04d46 (V8 clean trace work).

Let me check.

Confirmed: "one more raw log.txt" is essentially the same session content pasted into "another raw log.txt" (with 12 extra header lines listing attached files, and the outer session adds its own reads at lines 400-421). So "one more raw log.txt" = the standalone export of the handoff/V8-apply session; "another raw log.txt" = session arena/01a04d46 that includes that content as pasted context plus its own work.

Now let's read "another raw log.txt" from line 426 onward — the new session's work (V8 clean trace design). Let me look at the technical content.

This is rich. Net-new key facts from "another raw log.txt" (session 01a04d46):

    Restore gate: VA 0x1407862e1 call IsActiveForPlaythrough, 0x1407862e6 test al,al, 0x1407862e8 je 0x1407862f7 (raw 0x007856e8, still 74 0d in V8), 0x1407862f2 call UpdateFeatProgress.
    Note: "The older abandoned feat candidate did patch this location" — feat-V7 patched raw 0x007856e8? Let me check BANNED_ARTIFACTS to confirm feat-V7's two edits.
    Pre-V8 cache evidence: user_id=84696387, conquerer_from_bribir=1, established=2 (interesting: established=2 in this log vs 4 archived peak!) — a contradiction/observation to record.
    Preflight doesn't register V8 hash (already noted?).
    Evidence-snapshot PowerShell approach.
    The 3-case comparison matrix.
    Interpretation table.

Let me continue reading 700-1200 of "another raw log.txt".

HUGE finding! There are MULTIPLE cache files with DIFFERENT user_id values: 84696387 and 1148909174 (and earlier 453496064, 1179784490). So the feat cache is per-user_id — multiple cache files in the cache folder (all named with different random token names? Actually the loop copies every file in cache/ that has user_id= — so there are multiple cache files, e.g. q847rsja8ndx and others).

Wait — the loop prints "CACHE: " for each file. Only one CACHE line printed (q847rsja8ndx) then the subsequent blocks are... hmm, the output shows the CACHE header once, then a bunch of blocks. Actually the paste seems to be the user pasting content of multiple cache files. Let me look more carefully: after the first cache print (user_id=84696387, established=2, category=1474319405), then "$SaveDir ... Pavao/Croatia saves:" then the user pasted 4 more blocks (probably from separate cache files they opened manually). So there are several cache token files with different user_ids:

    user_id=1148909174, conquerer_from_bribir=1, established=4, category=-1991027533
    user_id=1148909174, conquerer_from_bribir=1, established=2, category=1474319405
    user_id=84696387, conquerer_from_bribir=1, established=2, category=1474319405
    user_id=1148909174, ... (truncated)

Let me see the continuation to get the rest, plus the file names.

Very rich. Additional cache states:

    q847rsja8ndx: MODIFIED 08/29/2026 16:19:51, SHA256 3606E210F48EB16B668B06E24942B545E65B11531F28F28D08FF9FDDE404601F, user_id=84696387, conquerer_from_bribir=1, established=3, category=-852858316.
    Other pasted blocks: user_id=1148909174 established=4 category=-1991027533; user_id=1148909174 established=2 category=1474319405; user_id=84696387 established=2 category=1474319405; user_id=84696387 established=3 category=-852858316.

Also key user observation: "tried steps. so, actually, even when i resign and load back, feats doesn't progress if i do things (probably was always like that). other problems remained"

Let me continue reading 1300-1800.

This is extremely relevant to the user's current question! Key evidence:

"i tried on different saves. when i resign then load feats remain, but dont go up. i loaded first day save, had already bronze (highest what i got) score. when completely go out of game, then load, 0 everywhere - in mj tab and ingame too."

This is exactly the phenomenon the user is now describing (in the current message): loading an earlier save shows already-Bronze feat icon even though conditions weren't completed in that save — because the cache stores the PEAK (SetCachedProgressIfHigher) and is profile/account-wide (keyed by user_id), not per-save.

So: the cache q847rsja8ndx is a local, per-user_id key-value store (feat_progress_storage) that stores peak progress; it's read at main menu and used for the "highest tier reached" display, so the Bronze icon shows for a fresh/early save. The "world heard about your deeds" popup fires on the transition when a tier is newly reached (SetCachedProgressIfHigher returning true / tier increase). Since the cache already has the peak, no new tier → no popup.

Now let me continue reading this log (1620-2200) and then the last log (for now).txt.

Let me continue.

Continuing. Let me read 1900-2400 of "another raw log.txt".

Continue reading 2250-2700. This is where the user pastes something ("anyway. look at this").

Interesting: register dumps showing R10 = "hethum_armenia" and "pavao_croatia" — payload ruler keys in memory at RIP 0x00007FF7B78C4970 (module offset = 0x78C4970 - 0x6EE0000 = 0x9E4970? Let's compute: base 0x7FF7B6EE0000 (from earlier). 0x7FF7B78C4970 - 0x7FF7B6EE0000 = 0x9E4970. So raw offset = 0x9E4970 - 0xC00 = 0x9E3D70, VA = 0x1409E4970. Interesting.

Let me skip ahead past the register dumps in "another raw log.txt" to the remaining prose (lines 2600-3743).

The register dumps occupy a lot. Let me find the last prose part of "another raw log.txt": search for lines after the register dumps.

Let me find where the prose resumes in "another raw log.txt" — after the last register dump. Search for the line numbers of "[MJ]" or prose sections after line 2800.

The register dumps go on. Let me find the first line after 2250 where a longer prose line appears (length > 60, not a register line).

Great — more net-new facts:

    Line 3233: Процесс запущен: 00007FF7B6EE0000 C:\Users\UZWERG\Desktop\SteamCrusader\CK2game.exe → confirms ASLR base 0x7FF7B6EE0000.
    Line 3237: "Точка останова 00007FF7B78C678B была отключена, так как байты не совпадают! Ожидается: 75 2F, получено: EB 2F" → breakpoint at 0x7FF7B78C678B (offset 0x9E678B → VA 0x1409E678B) disabled because bytes don't match: expected 75 2F, got EB 2F → this is the V7 Continue patch (0x009E5B8B: 75 2f → eb 2f) confirmed in memory! Wait, VA 0x1409E678B corresponds to raw 0x9E5B8B (0x9E678B - 0xC00 = 0x9E5B8B). Yes! This is direct runtime proof that the V7 Continue patch bytes (EB 2F) are present in the running process. Excellent evidence.
    x64db

I have a good grasp of "another raw log.txt". Let me check its ending prose (after the trace) — the last ~100 lines I already read (PR #15 creation and merge). Let me check lines 3440-3700 for the AI's analysis of the pasted x64dbg log.

Let me read lines 3478-3545 in full for the analysis of the old-breakpoints log (net-new details about the old Continue breakpoints in the x64db database).

Now let's read "last log (for now).txt" (6373 lines) — the V9 session, the most important one. Let me map it first: find the user turns and key sections.

This is the V9 session log. The V9_COLD_LOAD_FEATS_FIX.md and the handoff already capture the conclusions. But there may be net-new details: e.g., the DAILY_GATE breakpoint at +0x666146 (VA 0x140666146, raw 0x665546) is NOT the patched site (raw 0x666546) — a discrepancy worth noting! Let me read the relevant analysis around lines 200-260 and where the session resolves the DAILY_GATE offset confusion.

Wait, look: "DAILY_GATE +0x666146 → VA 0x140666146" — but the V8 patch is at raw 0x00666546 → VA 0x140667146. And the trace bp rip was 00007FF7B7546146 with base 0x7FF7B6EE0000 → offset 0x666146 → VA 0x140666146. Hmm, so the breakpoint is at VA 0x140666146, i.e., raw 0x665546 — a DIFFERENT location from the patched 0x666546. That's a 0x1000 difference. That's a significant finding: the trace script's DAILY_GATE breakpoint was set 0x1000 below the actual patched gate. Yet the log shows DAILY_GATE hitting with al=80/al=A0.

Hmm, but V9_COLD_LOAD_FEATS_FIX.md says: "V8 ran: DAILY_GATE (raw 0x666546 74 0d→90 90) executed and UPDATE_ENTRY fired". That conflates the breakpoint at +0x666146 with the patched site at raw 0x666546. Actually wait: raw 0x666546 → VA = raw + 0x140000c00 = 0x140667146, module offset = VA - image_base(0x140000000) = 0x667146. So the breakpoint at offset 0x666146 is NOT the patched site; the patched site is offset 0x667146. So there's a real discrepancy: the trace's DAILY_GATE bp is at offset 0x666146 (VA 0x140666146, raw 0x665546) — 0x1000 before.

This is a potentially important correction! Let me verify by reading the session's own analysis in "last log (for now).txt" — did they notice and resolve it? Let me read lines 200-420 and search for "0x1000" or "wrong offset" or "666146" mentions later.

Let me check the disassembly they produced at raw 0x665546.

This is a deep rabbit hole in the V9 session. The conclusion is documented in V9_COLD_LOAD_FEATS_FIX.md. Let me see how the session resolved the DAILY_GATE question — search for "666546" mentions later in the log and the resolution.

Let me grep for lines with "DAILY_GATE" and the resolution.

This is a significant finding: the DAILY_GATE breakpoint at CK2game.exe+666146 is NOT the patched DailyUpdate gate at raw 0x666546 (VA 0x140667146 = module offset +667146). It's 0x1000 earlier (raw 0x665546, mid-instruction in a different function — region A). This means the V8-clean trace's "DAILY_GATE" line did NOT prove that the V8 daily-gate patch executed. That contradicts the V9_COLD_LOAD_FEATS_FIX.md statement: "V8 ran: DAILY_GATE (raw 0x666546 74 0d→90 90) executed and UPDATE_ENTRY fired, so the daily caller did call UpdateFeatProgress on the cold path."

Wait — but the session may have resolved it. Let me read the resolution: line 600-660 and further. Let me check whether the session concluded DAILY_GATE is inside a different function that also calls UpdateFeatProgress (i.e., region A is a caller of UpdateFeatProgress), and whether the final V9 doc/handoff reflect the corrected understanding.

Let me look at the log's later parts to see the resolution, and search for what raw 0x665546's function is.

I need the resolution of the DAILY_GATE misplacement question. Let me search for how it ends up: search for "region A" and "0x665546" later, and the final conclusions of that session (near end).

Let me search for the section in the log where they conclude the DAILY_GATE bp identity, e.g. "so DAILY_GATE is" or "misplaced" or "wrong offset".

The V9 session established:

    The V8 clean-trace DAILY_GATE breakpoint was mis-set: script +666146 (raw 0x665546, mid-instruction) instead of +667146 (raw 0x666546, the patched je). It's in the same big function F1 (DailyUpdate) so it still signals "DailyUpdate ran", but it does NOT prove the V8 edit at raw 0x666546 was in place.
    The V9 trace script fixes it to +667146.

Note V9_COLD_LOAD_FEATS_FIX.md (already in the repo) says "V8 ran: DAILY_GATE (raw 0x666546 74 0d→90 90) executed" — this is an overstatement/incorrect claim; the correct statement is DAILY_GATE fired at raw 0x665546, inside the same function. This is a correction to record.

Let me check MJ_V9_CLEAN_TRACE.txt in the repo to confirm the fix (+667146).

Then read the rest of the V9 session log to capture net-new: V9 build chain (build_v9_chain.py), the eligibility loop analysis, the V9 test guide, etc. Let me look at the end of the V9 session log to see what was delivered.

Note: MJ_V9_CLEAN_TRACE.txt still uses bp CK2game.exe+666146 for DAILY_GATE (NOT fixed to +667146), even though the session log (line 1881, 2097) claimed it was "fixed in V9". So there's a discrepancy between the log's claim and the delivered file. That's a real finding — the V9 trace still has the mis-offset DAILY_GATE breakpoint. I should record that (and possibly fix it, since it's a harmless improvement: +667146 sits on the actual patched je).

Hmm — careful. Should I fix it? The user's request is: read logs, consolidate instructions, dissect into folders. Plus answer the V9/feat-icon question. Fixing the trace script would be a code change beyond scope, but it IS a "promising piece of info" — I could note it in a doc and optionally fix the script. Given the instructions say "guarded tooling" and "living docs repair is mandatory" — the discrepancy between the doc claim ("fixed in V9") and the actual file is a contradiction to record. I'll record it in CONTRADICTIONS.md and note the one-line fix (bp CK2game.exe+667146). I could also apply the fix... but changing the trace script changes its SHA and the README references. Better: record it and offer the fix. Actually, since the trace file's content matters little and the user might run it, a mis-set bp is harmless (it fires in the same function). I'll document it, and mention the exact edit. Let me not silently change tooling.

Now let me finish reading the V9 session log — particularly the end (deliverables, PR status, and what the final message said). Let me check the tail and the key sections.

The V9 session = arena/01a0534b, and it produced RUN_APPLY_CK2_MJ_V9_INLINE.ps1. Let me verify that file exists in the repo (per the "tooling integrity" rule — every file a doc names must exist).

Also let me check what other files the V9 session created and whether they're all in the repo: V9 ps1/bats, build_v9_chain.py, V9 test guide?, RUN_APPLY_CK2_MJ_V9_INLINE.ps1.

Let me grep the log for "Write" lines to list created files.

Important findings:

    RUN_APPLY_CK2_MJ_V9_INLINE.ps1 — the V9 session's last message claims it saved it to 05_patches_and_scripts/ps1/RUN_APPLY_CK2_MJ_V9_INLINE.ps1, but it does NOT exist in the repo (ps1/ only has patch_ck2_mj_v9.ps1 for V9). This is a tooling-integrity violation (per rule: "every .bat/.ps1/.py STATUS mentions exists"). Let me verify with a find across the whole repo.

    Git history: only one commit 82b3210 "Add files via upload" — the local clone is shallow (depth 1). Let me check git log --all and whether there's a remote with more history.

    There's no V9 test guide in 04_test_guides_and_reports (V5-V8 guides exist, no V9).

Let me verify these.

Confirmed: RUN_APPLY_CK2_MJ_V9_INLINE.ps1 was promised in the chat but never written (no Write... line in the log for it, and the file doesn't exist). This is a genuine tooling-integrity gap → the inline V9 patcher exists only in the raw log. I can reconstruct it from the log (it's verbatim in the log tail) and save it as a file — that's exactly what the instructions call for ("preserve every useful piece"; "Runnable tooling integrity: every .bat/.ps1/.py referenced exists"). I'll extract it verbatim from the log into 05_patches_and_scripts/ps1/RUN_APPLY_CK2_MJ_V9_INLINE.ps1.

Hmm — but the rule also says unguarded one-liners are not deliverables; this inline script IS guarded (hash checks, backup, verify). So it qualifies. And it's the exact tool the user actually used to get to V9 (they said "v9 works"). Let me verify: does patch_ck2_mj_v9.ps1 exist and is it equivalent? Yes it exists. The inline one is a no-file-needed version. I'll save it since the log promised it and it's the tool the user used.

Let me now check remaining important parts of the V9 session log I haven't read: the middle sections with the analysis (disasm outputs, strings, the feat cache mechanics, "world heard" / popup mechanics). In particular, for the user's question about the Bronze icon + "world heard about your deeds" message, I want to find the relevant game strings/events in the payload or strings dump.

Let me grep the repo's game data for relevant loc keys: "world", "deeds", "bronze", "feat" messages.

The "world heard about your deeds" message is likely a Monarch's Journey notification. Let me search the strings dumps for MJ-related notification text — e.g., "Monarch's Journey", "Bronzeman", "feat", "tier". Let me grep ck2_strings.txt / CK2game_strings.txt for relevant keys.

Also check the payload monarchs for the feat definitions (33 challenges) and any notification keys.

Let me search for "MJ_" or "monarchs_journey" or "bronze" in the strings._

Good. The payload has 11 rulers with feats_script containing levels = { a b c } (Bronze/Silver/Gold thresholds) and set_variable/change_variable on global_<feat>._

Now, for the user's question about "world heard about your deeds" message and the Bronze icon persisting: I need to find the MJ notification message. Let me search the strings for keys like "MJ_" or "HIGHLIGHTED_RULER" text. Let me grep the loc CSVs for "highlighted_ruler" or "featured"._

Also important: the archive's F9/P-notes about "Bronze tier grant popup" (case C22) — "V6 live eval (Pavao established)". The popup message: let's find its text key.

Let me grep strings for "Bronzeman" and MJ popup keys.

Let me find the actual message text for the Bronze tier grant — the user recalls "world heard about your deeds". Let me search strings for MJ-related notification like "Your deeds" or "The world". Search ck2_strings.txt for "deeds" and "world".

Also search for feat-tier loc keys: "FEAT_" or "MJ_".

Let me also check V6_RUNTIME_RESULTS.md for the game.log line of the Bronze popup — it may contain the exact event/notification text.

PERFECT. This is the answer to the user's question:

The message is:

Congratulations!
Word has spread far and wide of your achievements.
You earned the rank of Bronze in the "Established" challenge.

So "world heard about your deeds" = "Word has spread far and wide of your achievements. You earned the rank of Bronze in the "Established" challenge."

And the mechanism: the local feat cache q847rsja8ndx stores the PEAK per feat (established=4 = Bronze threshold peak), keyed by user_id, not per-save. So when loading an earlier save (e.g. day 1 with established=2) the icon already shows Bronze because the cache peak (4) ≥ Bronze threshold (4). And no new rank is granted because the rank was already granted (SetCachedProgressIfHigher only fires the popup when a NEW higher tier is reached).

Let me verify the "cache stores peak" claim and the mechanism in the archive: V6_RUNTIME_RESULTS §5 says "the cache below stores the peak". And symbol CFeatProgressStorage::SetCachedProgressIfHigher (Linux 0xf47534) — from the handoff. Let me check whether the archive has the function list. Also CheckNeedsCache, ReadCachedProgress, SetNewProgress(CString,int,bool,bool), Update.

Let me grep the repo for SetCachedProgressIfHigher to confirm it's recorded.

Now, is the tier icon driven by the cache or by the in-game counter? Evidence:

    User: "when i resign then load feats remain, but dont go up. i loaded first day save, had already bronze (highest what i got) score." — the first-day save has global_established=2 (below Bronze=4), yet the icon shows Bronze. That proves the displayed tier comes from the cached peak, not the save's current value.
    Also: "when completely go out of game, then load, 0 everywhere" (V7 cold) — that was the cold-load bug, now fixed by V9.

So for the current question: the icon showing Bronze on an earlier save/new campaign is EXPECTED behavior of the original feature: the cache is per-account (local user_id) and stores lifetime peaks. It is not tied to a save. The congratulations popup only fires when a feat crosses into a NEW tier above the cached peak.

Let me now verify the exact semantics of the feat cache file naming: q847rsja8ndx is a key in cache/ folder. Multiple cache files with different user_id values existed (453496064, 84696387, 1148909174, 1179784490). Hmm — actually maybe those were different files (each cache token file name is random). The user pasted several blocks — some may be from other cache token files in the cache folder.

Wait, important: the archive says "cache/q847rsja8ndx" is the feat_progress_storage. But the multiple user_ids suggest multiple files. In the "another raw log" session, the AI said "The additional blocks containing user_id=1148909174 need not be current. Your command showed only one current cache file. They may be older copied outputs." So the enumeration showed only ONE file with user_id: q847rsja8ndx. The other blocks were older copies (from earlier pastes/snapshots). Good.

So known cache states (chronological-ish):

    453496064 / heretical_company=1 / established=0 / category=697115649 (Bosnia/Kulin era)
    84696387 / established=4 / conquerer_from_bribir=1 / category=-1991027533 (v6 second look)
    84696387 / established=2 / category=1474319405
    84696387 / established=3 / category=-852858316 (modified 2026-08-29 16:19:51, SHA 3606E210…)
    84696387 / established=3 / category=-852858316 (modified 2026-08-29 20:32:18, length 751)
    1148909174 / established=4 / category=-1991027533
    1148909174 / established=2 / category=1474319405
    1179784490 / established=4 / conquerer_from_bribir=1 / category=-1991027533 (from "one more raw log")

Interesting: category varies but the feat values match across user_ids — suggesting user_id is not stable across boots (identity drift!) — the values are the same, so these are likely the same logical cache written under different user_id values (i.e., the offline user_id is derived from something that changes). The archive says "Identity drift refuted (preflight user_id=84696387 stable)" but this new evidence shows three distinct user_ids (84696387, 1148909174, 1179784490) with identical feat vectors — that contradicts "identity drift refuted" and is worth recording in CONTRADICTIONS.md as re-opened evidence. Note the archive also mentions 453496064 as a different capture.

Hmm, careful: it says the enumeration of the cache folder showed only ONE file with user_id. So the different user_id blocks were pastes of the same file at different times → the user_id VALUE CHANGED over time for the same file. That is identity drift (or at least user_id instability). This is a genuinely important, net-new, potentially-unrecorded finding. Let me check FEAT_RESET_QUIT_VS_RESIGN.md to see what it says about 453496064 vs 84696387.

Let me read that file.

Now I have the full picture. Let me now check remaining files I haven't inspected: the .patch file (01a044b2...patch) — it's already merged (PR #13) and the same file also exists in 11_git_patch/. Let me verify by comparing hashes.

Also, I should check the rest of the "last log (for now).txt" for net-new analysis facts that are NOT yet in the archive. Key candidates:

    The eligibility loop analysis (in V9 doc: yes).
    The 0x14072c6c0 / 0x14072d010 details (in V9 doc: yes).
    The strings dump / xref scan results (maybe net-new).
    The V9 build chain script (build_v9_chain.py in repo).
    The disasm*.py scripts (disasm.py … disasm25.py in repo py/).
    Whether the log records the "special_event"/gameState+0x598 conclusion being disproven (V8's premise). Note: V9 doc says the gate chain, but does anything record that the "special_event not restored on cold load" theory was refuted? The V9 root cause is different: the eligibility gate returns 0 because feature-availability flags aren't populated. Actually IsActiveForPlaythrough's result is bypassed in V8 anyway.*

Let me grep the V9 session log for key conclusions I should preserve: search for "REFUTED", "refuted", "CONCLUSION", "net-new", "special_event".

Let me also check the middle part of the V9 log (lines 1500-2100 and 2400-3300) for analysis output that isn't in the archive. Given time, let me grep for distinctive technical strings.

There's a rich hypothesis around 0x17a36d62 (the expected special-event hash / id) and [+0x63] write. That is potentially net-new info not in the V9 doc. Let me read lines 2150-2400 to capture this properly, and check whether the V9 doc's "The only [+0x62]=1 write ... raw 0x767cb3 ... recommended_dlc_list, YOUKICKED, ld" matches.

Note the V9 doc says [this+0x62] is irrelevant — the "blocked" flag written by the recommended_dlc_list/YOUKICKED matcher at raw 0x767cb3. And [this+0x63]!=0 is required. So the +0x63 write is the key. Let me read the relevant analysis.

This is very rich net-new analysis: the "playthrough activation" function at VA 0x14080f370 that sets singleton+0x65 and +0x63 = (id == 0x17a36d62). This is NOT in the V9 doc or the handoff. It's a genuinely promising lead for future work (e.g., if V9 hadn't worked, or for a cleaner fix).

Now let me look at more of the log to catch other net-new items, especially near the end (V9 build/verification and any user-reported results), and the section where they verified the V9 chain hashes.

Let me grep for the V9 verification output (SHA values, chain).

Good. This is all consistent with the V9 doc. The net-new items I've gathered are substantial. Let me also check the section where the V9 session considered option (A) vs (B), and the "alternative V9" (raw 0x7b7854 forcing the global flag) — that's a useful fallback recorded nowhere. Let me confirm: is the alternative (A) recorded in the archive? grep for "7b7854".

Also check: is 0x14080f370 recorded anywhere in the archive? grep.

Net-new (not in any doc):

    0x17a36d62 magic special-event id and the activation function VA 0x14080f370 (only in disasm18.py as an address, no analysis text).
    The multiple user_id values (1148909174, 1179784490) and the cache file SHA 3606E210…
    The old Continue breakpoints at +0x9E4970/+0x9E5500/+0x9E5B8B (only +0x9E5B8B is documented)
    PR #13/#14/#15 numbers, merge commits 96ba84b / 910234875… / f29287217…, session branch IDs (01a04980, 01a049a4, 01a04d46, 01a0534b)
    The DAILY_GATE mis-offset finding (bp +666146 vs patched +667146)
    The saves list with sizes/timestamps (Croatia1278_01_02..08, Bronzeman_pavao_croatia 1..3) and their global_established values
    The x64dbg database path, db file CK2game.exe.dd64, load time 1703 ms
    The "Word has spread far and wide of your achievements" text is already in V6_RUNTIME_RESULTS (good).
    The V8 side effect: main-menu feats zeroed under V8 (user report) — is that recorded? Let me check. grep "main menu" + "0" in docs. The STATUS says "user tried the V8 files and reported 'still didn't work'". The V8 zeroed main menu is in the raw log — let me check if documented.

Let me check a few of these.

Now I have enough understanding of the material. Time to plan the deliverables.
Plan
Deliverable 1: One continuous consolidated instruction document

Create ALL-MY-LOGS-SO-FAR/00_START_HERE/MASTER_ARCHIVE_RULES.md (or name it PROMPT_organize_research_log_v7.md?). The user said: "find the instructions (in its own folder) about structure, process of 'tearing apart', storing and preserving every useful, interesting, promising, etc piece of info. first collect them into one as one continuous thing."

So: a single continuous document that merges v4 (raw-log dissection A–F + 12 rules), v5 (ingestion/maintenance, artifact-class→folder map, dedup, living docs, integrity), v6 (state dashboard, naming collisions, Continue surfaces, tooling integrity, trace distillation, git-patch provenance, saves metadata) into one operative master doc — call it PROMPT_organize_research_log_v7_MASTER.md or MASTER_ARCHIVE_RULES_v7.md. I'll name it PROMPT_organize_research_log_v7.md (following the versioning convention, and README says "v6 is the operative one") and update README/ORGANIZATION_HISTORY to point at v7 as operative. Actually, to make the "one continuous thing" obvious, I'll title it "MASTER INGEST & DISSECTION RULES (v7 — consolidated)". I'll keep the file name PROMPT_organize_research_log_v7.md for convention consistency, and inside note it supersedes v4/v5/v6 as the single continuous text.
Deliverable 2: Dissect "last log" into appropriate folders

Per the rules:

    Part 5 research archive → 01_research_archives/CK2_MJ_RESEARCH_ARCHIVE_PART5.md following the A–F skeleton (V8 test → clean trace → V9 build → V9 result). Must include verbatim hashes/offsets, artifact table, patch map, attempts/dead ends, open threads, context.
    Net-new extracts → append to 03_analysis/RAWLOG_NETNEW_EXTRACTS.md (§11 …) with byte-level extras: activation function 0x14080f370 + 0x17a36d62, old Continue bps, cache states incl. new user_ids and SHA, save list with global values, x64dbg environment details, DAILY_GATE mis-offset.
    V9 runtime results → new 03_analysis/V9_RUNTIME_RESULTS.md (user's V9 verdict: works; feats present; survive soft & hard quit; increase on correct actions; open question: bronze icon on earlier save + missing popup). Also add the feat-cache/peak-tier explanation.
    Feat cache deep-dive → new 03_analysis/FEAT_CACHE_PEAK_AND_TIER_ICON.md explaining the user's question: cache is per-user (local key-value, peak per feat), not per-save; tier icon reflects peak; popup only on new tier; how to reset (delete/rename cache file — NEVER wipe_feats console command; deleting the cache file is safe & reversible; back it up first), and the evidence (first-day save with established=2 showing Bronze; the "Word has spread far and wide…" text from V6_RUNTIME_RESULTS §3; SetCachedProgressIfHigher symbol).
    Test guide → 04_test_guides_and_reports/CK2_MJ_V9_TEST_GUIDE.md? Actually V9 was already applied and works. Maybe instead a "V9 result + next tests" doc. I'll add 04_test_guides_and_reports/CK2_MJ_V9_TEST_GUIDE.md with the actual result recorded + the cache-reset test procedure. Hmm, test guides are "in-game click paths". A V9 test guide + result is appropriate.
    Tooling: reconstruct the promised-but-missing 05_patches_and_scripts/ps1/RUN_APPLY_CK2_MJ_V9_INLINE.ps1 verbatim from the log (tooling integrity rule). Also record the MJ_V9_CLEAN_TRACE.txt DAILY_GATE offset note (don't silently change; document).
    13_save_and_cache: add the new cache states observed (as text records; no binaries). Maybe append to README.md there: table of observed cache states incl. new user_ids and the 2026-08-29 SHA. And the save inventory (10 saves with sizes/dates/global values) — as metadata (saves not uploaded).
    Living docs: STATUS.md (V9 = new baseline, proven; open item = tier icon/popup; C-cases update), CASES_AND_FINDINGS.md (C26? new case for "bronze icon on earlier save / no popup" → PARTIAL/INFO; update F9), PLAN.md, README.md (read order + v7 operative), 12_raw_chat_logs/INDEX.md (ledger entries for the 11 files in "last log"), MASTER_ARTIFACT_TABLE.md (new artifacts: the 11 last-log files with SHA + role; V9 tooling), CONTRADICTIONS.md (identity drift re-opened; DAILY_GATE claim vs actual bp; V9 doc's "DAILY_GATE executed" overstatement; MJ_V9_CLEAN_TRACE claim "fixed" vs file).
    ORGANIZATION_HISTORY.md: add Turn entry for this ingest.
    The "last log" folder itself: per convention, raw staging gets dissected then deleted (git rm). The patch file is a byte-identical duplicate of the archived one in 11_git_patch/ → discard. The trace txt/bat are byte-identical? Let me check against 05_patches_and_scripts/x64dbg/MJ_V8_CLEAN_TRACE.txt and RUN_MJ_V8_CLEAN_TRACE.bat. If identical → duplicates.

I should ask? The instructions say delete raw files after dissection (git history retains). The user said "first collect them into one as one continuous thing. and then dissect into parts that would go into apropriate folders." That implies moving content into folders. Deleting the raw "last log" folder is the documented convention, but risky if the user wants to keep it. Hmm. The rules say: "New raw chat export | dissect then delete". I'll follow the convention but... deleting 1.9 MB of the user's logs could upset them. The prior sessions did exactly that (deleted after dissection) and it's documented as the pattern, with git history retaining. But this repo's git history is shallow (depth 1) locally — actually the remote has full history; but the "last log" folder was uploaded by the user in commit 82b3210, so it's in git history on the remote.

Safer: I'll keep the raw files but... no. Let me follow the repo's own documented rule but be explicit and reversible: I'll dissect everything, then git rm the raw chat exports (keeping the byte-identical duplicates removed) — hmm.

Actually, let me weigh: the user's instruction is "dissect into parts that would go into appropriate folders". They didn't say "delete the last log folder". The archive rules say dissect then delete. Since deletion is destructive and the user is non-technical, I'll do the dissection completely, and NOT delete the folder — instead I'll note in the INDEX ledger that the raw files are dissected and can be deleted, and ask/offer. Hmm, but then the repo has duplicate content (the patch file is an exact dup of 11_git_patch copy; MJ_V8_CLEAN_TRACE.txt dup of x64dbg copy).

Decision: I'll ask the user? No — the turn should deliver. I think the best approach: follow the convention (it's explicitly documented and repeatedly applied in this repo: "all raw exports were torn apart & deleted; their info lives in Parts 1–3"), i.e., dissect then git rm the 9 raw chat exports + the 2 duplicate artifacts, and record every file in the INDEX ledger with where its content went. Everything is recoverable from git history (the folder is in commit 82b3210 on the remote, and my commit will be on the branch).

Hmm, but "Never delete... unless required". The system prompt says never delete the repo root or .git. Deleting user files that they explicitly uploaded... The repo convention says to. Risk: if the user is unhappy, git history restores.

I'll be conservative-but-conventional: keep the raw files in place for now (do not delete), but record in INDEX.md that they are fully dissected with the "may be deleted" status, and explicitly tell the user in my reply: "say the word and I'll delete the folder per the archive rule". That respects both. Actually... the instructions the user asked me to follow say delete. Hmm, "dissect then delete" is in the v5/v6 table. The user asked me to follow those instructions.

Compromise that honors both: dissect fully; then delete only the two files that are byte-identical duplicates of already-archived copies (the .patch and MJ_V8_CLEAN_TRACE.txt + RUN bat if identical) — no wait, deleting a duplicate is safe by the dedup rule ("Byte-identical → discard"). And keep the raw chat exports with a ledger entry marked "dissected — safe to delete on user confirmation". I'll present the choice clearly. Hmm, that's wishy-washy.

Let me think about what maximizes user value: The user wants their info preserved and organized. Deleting the raw logs doesn't add value; it just follows a rule whose purpose was workspace budget (128 MB / 10K files). The folder is 1.9 MB. No budget pressure. The real risk of keeping them is duplication/drift — mitigated by the INDEX ledger marking them dissected.

Final decision: Keep the raw files, mark them dissected in the INDEX ledger with a note that the archive's delete-after-dissection rule can be applied on request (and that the .patch is a byte-identical duplicate of the archived copy). I'll state this clearly in my reply. This is defensible and non-destructive.

Hmm, but then "dissect into parts that would go into appropriate folders" — maybe the user literally wants the files moved into appropriate folders too (e.g., the patch → 11_git_patch, the trace files → 05_.../x64dbg). Since the patch and trace files are exact duplicates of files already in those folders, "moving" = deleting the duplicate. That's the dedup rule. OK:_

    last log/01a044b2-…patch → byte-identical to 11_git_patch/01a044b2-…patch → duplicate, discard (delete).
    last log/MJ_V8_CLEAN_TRACE.txt → check vs 05_patches_and_scripts/x64dbg/MJ_V8_CLEAN_TRACE.txt.
    last log/RUN_MJ_V8_CLEAN_TRACE.bat → check vs x64dbg copy.

Let me verify those two now.
Deliverable 3: Answer the V9 / bronze-icon question

In my reply + a doc.

Let me also double-check the claim "cache applies to every save unless deleted" by looking for evidence in the logs/archive:

    The cache is read at main menu and used for the "peak" display: FEAT_RESET doc: "The main menu is unaffected because it reads the local cache cache/q847rsja8ndx (via CFeatProgressStorage::ReadCachedProgress)".
    User evidence (2026-08-29): "i loaded first day save, had already bronze (highest what i got) score" while that save has global_established=2 (Bronze threshold = 4) → icon from cache peak, not from save.
    The congratulations text fires on rank grant (game.log line in V6 results).
    Symbol evidence: CFeatProgressStorage::SetCachedProgressIfHigher (from the handoff's address list, Linux 0xf47534) — "if higher" = peak semantics. And CheckNeedsCache, SetNewProgress(CString,int,bool,bool).

Let me verify SetCachedProgressIfHigher is in the strings dump (ck2_strings.txt has Linux symbols). Let me grep.

Also verify the exact loc key for the congratulations message if present in strings — search for "Word has spread far and wide" in ck2_strings.txt.

Let me run those checks.

CFeatProgressStorage::SetCachedProgressIfHigher(CString const&, int) — confirms peak semantics ("if higher").

The "Word has spread far and wide of your achievements" text isn't in the strings dump (it's probably in the loc CSV for MJ, or built from a notification template). Let me search the loc CSVs and the payload's add_localisation. Let me grep LT.csv / RedKing.csv for "spread far and wide".

Also search the game.log text in V6_RUNTIME_RESULTS (already have it).

Let me check the payload's add_localisation for the message.

Excellent! Found the exact loc keys in 06_game_data/RedKing.csv:

    FEAT_COMPLETE_LOG = "\nCongratulations!\nWord has spread far and wide of your achievements.\nYou completed the challenge "$FEAT$"."
    FEAT_LEVEL_1_COMPLETE_LOG / FEAT_LEVEL_2_COMPLETE_LOG / FEAT_LEVEL_3_COMPLETE_LOG = "…You earned the rank of $LEVEL$ in the "$FEAT$" challenge."

So the message the user remembers is FEAT_LEVEL_1_COMPLETE_LOG (Bronze = level 1). This is a log/notification fired when a feat level is newly completed.

Let me look at more of the FEAT_* loc keys in RedKing.csv to see the full set (this is great supporting evidence for the tier/popup mechanics).*_

PERFECT. This is definitive evidence for the user's question:

    FEAT_LEVEL_1_COMPLETE_SETUP = "Challenge Rank Bronze Earned" (the notification title)
    FEAT_LEVEL_1_COMPLETE_LOG = "\nCongratulations!\nWord has spread far and wide of your achievements.\nYou earned the rank of $LEVEL$ in the "$FEAT$" challenge." — the message the user remembers as "world heard about your deeds".
    FEAT_HIGHSCORE_TT = "Best Result: $HIGH_SCORE|G$" ← THE SMOKING GUN: the UI has a "Best Result" tooltip = the cached peak. This confirms the cache stores the best/high score and the UI displays it separately from current progress.
    FEAT_CURRENT_TT = "Current Progress: $SCORE|Y$" — current vs best distinction.

So: the challenges tab shows current progress (from the save's globals) AND "Best Result" (from the cache peak). The tier icon (Bronze/Silver/Gold medal) reflects the highest rank ever earned for that challenge, i.e., the cached peak — not the current save's state. Hence loading an earlier save (or restarting a campaign) still shows Bronze, and no new "Word has spread…" notification fires because the rank was already recorded as earned (the notification fires only on a newly completed level, tracked by the cache).

This fully explains the user's observation and confirms their hypothesis ("cache file applies to every save unless deleted"). The cache is keyed by local user_id, not by save file — it lives in Documents\Paradox Interactive\Crusader Kings II\cache\q847rsja8ndx.

Also I should note: deleting/renaming that cache file resets the "best result"/tier icons — that's the supported way to start clean (NOT wipe_feats, which is the banned console command; though note wipe_feats is the console version of clearing the cache — the rule says never run it, mainly because the console is disabled in Bronzeman and because it destroys progress. I should be careful: recommending deleting the cache file is effectively the same as wipe_feats. The rule "never run wipe_feats" exists because it destroys progress irreversibly. So I must recommend: back up the cache file first, then rename it; and note that this loses the recorded best results. Also the feats themselves live in the save's globals, so the save progress isn't lost — only the "best result"/rank record.

Let me verify the claim "the medal/tier icon comes from the cache rather than the save" more strongly. Evidence:

    FEAT_HIGHSCORE_TT = "Best Result" exists as a separate UI element → engine tracks a high score separate from current.
    CFeatProgressStorage::SetCachedProgressIfHigher → writes only if higher (peak).
    User's own observation: first-day save (global_established=2, Bronze threshold 4) showed Bronze already.
    V6_RUNTIME_RESULTS §4 note: "established is a state-based feat... can decrease (4 → 3); the cache below stores the peak."

That's solid. I should be honest that point 3 is the user's report and the mechanism is inferred from symbols/loc keys (not from a live disassembly of the icon-drawing path). Mark it as "high-confidence inference" vs "verified".

Now — one more thing to check: does the tier icon in the MJ panel come from GetFeatLevel (CRulerFeatTracker::GetFeatLevel, lin 0xff7c68)? The handoff lists it. The tracker has two vectors: +0x108 current, +0x120 cached. So the UI can select between current and cached — consistent.

OK. Now let me also confirm the user's V9 claims are recorded nowhere yet (STATUS says V9 ready, pending). Right — STATUS says "V9 ready... needs one cold rerun to confirm". Now the user reports it works. So STATUS/CASES/PLAN/handoff must be updated → "living docs repair is mandatory".

Let me now check a couple more things before writing:

    The MASTER_ARTIFACT_TABLE format (to append rows).
    The RAWLOG_NETNEW_EXTRACTS format (to append §11).
    13_save_and_cache/saves contents.
    README of 05_patches_and_scripts (README_PREFLIGHT) - maybe not needed.

Let me look at the tail of RAWLOG_NETNEW_EXTRACTS.md and MASTER_ARTIFACT_TABLE.md structure.

I have everything I need. Let me also quickly check section 5 (Local feat-progress cache) of MASTER table and section 6 (toolchain) so I can append V8/V9 rows properly. And check whether V8/V9 hashes are in MASTER table already.

Then write the deliverables. Let me check.

MASTER_ARTIFACT_TABLE lacks V8/V9 hashes → living-doc repair needed (mandatory per rules).

Now let me compute SHA-256 of the V8/V9 tooling files so I can add them to the MASTER table.

Then write everything. Let me compute the hashes for the V8 and V9 toolchain files.

Note: x64dbg/MJ_V8_CLEAN_TRACE.txt SHA = 49c4a602… matches the "last log" copy exactly (confirming dup). Same for RUN bat (5b2e79de…).

Let me get the remaining hashes with correct paths.

Now, time to write. Let me plan the files:

A. Consolidated instructions (one continuous thing): ALL-MY-LOGS-SO-FAR/00_START_HERE/PROMPT_organize_research_log_v7.md — "MASTER RULES — archive structure, tear-down, storage & preservation (v7, consolidated from v4+v5+v6)".

B. Dissection outputs:

    01_research_archives/CK2_MJ_RESEARCH_ARCHIVE_PART5.md — Part 5: V8 disproven → clean trace → V9 (A–F skeleton).
    03_analysis/RAWLOG_NETNEW_EXTRACTS.md — append §11 (last-log residue).
    03_analysis/V9_RUNTIME_RESULTS.md — new: V9 proven + user report + open item.
    03_analysis/FEAT_CACHE_PEAK_TIER_ICON.md — new: the bronze-icon/popup explanation (answers the user's question), with the loc keys, symbols, evidence, and the reset procedure.
    04_test_guides_and_reports/CK2_MJ_V9_TEST_GUIDE.md — new: what was run, results, and the two remaining checks (V9 clean trace; cache-reset test).
    05_patches_and_scripts/ps1/RUN_APPLY_CK2_MJ_V9_INLINE.ps1 — reconstructed verbatim from the log (promised but never written).
    13_save_and_cache/README.md — extend with the 2026-08-29/30 cache states + save inventory.
    Living docs: 00_START_HERE/STATUS.md, 00_START_HERE/CASES_AND_FINDINGS.md, PLAN.md, README.md, 03_analysis/MASTER_ARTIFACT_TABLE.md, 03_analysis/CONTRADICTIONS.md, 12_raw_chat_logs/INDEX.md, 00_START_HERE/ORGANIZATION_HISTORY.md, 02_handoffs/NEXT_SESSION_HANDOFF_2026-08-30.md (banner update to V9 proven).
    Maybe 00_START_HERE/DISSECTION_REPORT_2026-08-30.md — the v4 rule says: "If source is a cleanup folder with multiple session logs, also produce DISSECTION_REPORT.md summarizing each log's actions and preservation status." The "last log" folder has 6 distinct session logs → yes, produce DISSECTION_REPORT_2026-08-30.md.

Also INGEST_REPORT? The v6 output requirement: "Output: short ingest report." I'll put the ingest report in the DISSECTION_REPORT file (§ ingest report) and summarize in chat.

Let me be careful about scope/size. This is a lot of writing but that's the task.

Let me start with the consolidated instructions doc (A).

I need to write it as ONE continuous document: structure (15-folder map), tear-down process (v4 A–F + coverage sweep + dissect-or-verify), storing/preserving (dedup by SHA, net-new extraction, living docs, integrity, contradictions, binary policy, naming), plus the v6 additions. I'll organize it as a single continuous rulebook with numbered sections, and mark provenance (which version each rule came from) so nothing is lost.

Let me write it now.

Now I have the full picture of the material. Let me write the consolidated instructions document first.
WriteALL-MY-LOGS-SO-FAR/00_START_HERE/PROMPT_organize_research_log_v7.md+333

Now the Part 5 research archive. This is the big one. Let me gather the remaining facts I need — particularly the chronology of sessions and PRs, and the exact user quotes.

Sessions in "last log":

    first first raw log.txt (4358 lines) & raw chat.txt (4346) — session arena/01a044b2: diagnosis of feat-reset, V8 build (2 edits), preflight fix, CRLF fix; ended with rejected PR request. (raw chat.txt is a truncated re-export of the same session.)
    one more raw log.txt (426 lines) — separate chat (no repo access): user pasted NEXT_SESSION_HANDOFF_2026-08-29 + renamed patch files as .txt; V8 applied via PowerShell (backup CK2game.exe.before_mj_v8_from_v7_20260829_132333.bak), result "feats are 0 in game, and even in main menu (in MJ tab) too now"; cache paste (user_id=1179784490, established=4, conquerer=1, category=-1991027533); reverted; BAT flicker root cause = cmd parse of parentheses in if-block.
    first raw log.txt (497) — session arena/01a04980: landed the 01a044b2 patch → PR #13, merge commit 96ba84b; verified stock→V5→V6→V7→V8 hash chain; archived patch to 11_git_patch; deleted the then-current "last log" folder.
    another other raw log.txt (1142) — session arena/01a049a4: read raw chat + patch; determined everything already in b4cdb4a; user reported "i tried new files, but it still didn't work, unlike early ones"; wrote NEXT_SESSION_HANDOFF_2026-08-29.md (237 lines); STATUS edit; PR #14 (commit ab07419), merge commit 910234875decd988ce55ed95e2401ce0f8c1b02a.
    another raw log.txt (3743) — session arena/01a04d46: deep evidence session; V8 confirmed installed (State: v8, 94d6fb40…); cache enumeration (q847rsja8ndx modified 29.08.2026 16:19:51, SHA 3606E210…, user_id=84696387, established=3, category=-852858316; later 20:32:18, 751 bytes); multiple other cache states incl. user_id=1148909174; save list (10 saves) with global_established values; discovered RestoreDeviceObjects gate raw 0x007856e8 unpatched; user's key observation "even when i resign and load back, feats doesn't progress if i do things (probably was always like that)"; x64dbg old-breakpoint log analysis; created MJ_V8_CLEAN_TRACE + RUN bat; PR #15 squash-merged f29287217b300be83a0c6334ccddc9a780bd5092.
    last log (for now).txt (6373) — session arena/01a0534b: analyzed the V8 clean trace (x64dbg logs.txt), disassembled stock exe with capstone (disasm.py…disasm25.py), found the failing final gate 0x1400af690 at raw 0x7b7906, built V9 (build_v9_chain.py, full V2→V9 chain hash verification), delivered patch_ck2_mj_v9.ps1 + 3 bats + V9 trace + README + V9_COLD_LOAD_FEATS_FIX.md + NEXT_SESSION_HANDOFF_2026-08-30.md; gave the inline PowerShell patcher (promised file RUN_APPLY_CK2_MJ_V9_INLINE.ps1 never written).
    x64dbg logs.txt — the V8 clean trace capture (evidence for V9).
    MJ_V8_CLEAN_TRACE.txt + RUN_MJ_V8_CLEAN_TRACE.bat — byte-identical duplicates of the archived helper.
    01a044b2-…patch — byte-identical duplicate of the archived patch.

Wait: is #3 (first raw log) before or after #4 (another other)?

    #3 = arena/01a04980 landed PR #13 (V8 work) — branch ID 01a04980.
    #4 = arena/01a049a4 wrote the handoff — 01a049a4 > 01a04980, so #4 after #3. ✓ (Also #4's session says everything from session 01a044b2 is already committed in b4cdb4a "Add files via upload" — consistent with the user re-uploading after PR #13 merged... hmm, actually the branch base differs. Whatever — record both branch IDs and the ordering by UUID.)
    #5 = arena/01a04d46 (after 01a049a4) ✓ — it starts by pasting the handoff session content.
    #6 = arena/01a0534b (after 01a04d46) ✓.
    Current session = arena/01a053d9.

Also note #4's log says the repo HEAD was b4cdb4a, and #3's says HEAD was 5b39d7d. These are different "Add files via upload" commits — the user re-uploads the repo snapshot between sessions. Worth noting as an environment fact (history is squashed into single web-UI uploads; local clone is shallow depth 1).

Now let me write Part 5. It should be thorough. Let me include:

    A0 read-first with the updated naming table
    A2 dashboard
    A3 chronology with branch IDs and PRs
    B story/timeline/mental-model evolution (including the wrong theories: special_event/gameState+0x598 not restored → refuted as the operative cause; identity drift → re-opened)
    B3 lineage table V8, V9
    C knowledge base: gate chain, final gate, eligibility loop, activation function 0x14080f370 + 0x17a36d62, artifact table, patch map rows, cache states, save inventory, verified calculations (VA↔raw, je displacement check, ASLR base arithmetic)
    D attempts & dead ends
    E open threads (incl. the bronze-icon question, the DAILY_GATE bp offset, MJ_V9 trace fix, missing inline ps1 — now reconstructed)
    F context (environment, method lessons, error ledger, evidence inventory, preservation audit)

Verified calculations I should actually perform to satisfy rule 2:

    VA = raw + 0x140000c00 for the V9 site: raw 0x7b7906 → VA 0x1407b8506. ✓ (0x7b7906 + 0x140000c00 = 0x1407b8506)
    The call 0x1400af690 encoding at raw 0x7b7906: e8 + rel32 where rel32 = target − (VA_next). VA of call = 0x1407b8506, next = 0x1407b850b. 0x1400af690 − 0x1407b850b = −0x708E7B = 0xFF8F7185 as 32-bit two's complement → bytes 85 71 8f ff (little-endian) → e8 85 71 8f ff ✓ matches the documented pre-patch bytes. Let me verify numerically with python.
    DAILY_GATE: module+0x666146 → VA 0x140666146 → raw 0x665546; patched je at raw 0x666546 → module+0x667146. Difference 0x1000. ✓
    ASLR base from the trace: 0x7FF7B7546146 − 0x666146 = 0x7FF7B6EE0000 ✓; and RESTORE_GATE 0x7FF7B76662E8 − 0x7862E8 = 0x7FF7B6EE0000 ✓; UPDATE_ENTRY 0x7FF7B7698E60 − 0x7B8E60 = 0x7FF7B6EE0000 ✓.
    Old Continue bp 0x7FF7B78C4970 − 0x7FF7B6EE0000 = 0x9E4970 ✓; 0x7FF7B78C5500 → 0x9E5500 ✓; and 0x7FF7B78C678B → 0x9E678B ✓ (the V7 site, expected 75 2F got EB 2F).
    V9 patch length: 5 bytes → 5 bytes (b0 01 90 90 90) ✓ length-preserving.
    Cache file: 751 bytes at 20:32:18 vs the archived q847rsja8ndx_v6_secondlook.txt = 752 bytes. Interesting: 1-byte difference. The repo copy has CRLF? Let me check: the repo file is 752 bytes; the user's live file is 751 bytes. Probably a trailing newline difference. Worth noting as a minor discrepancy — actually, the repo's copy was pasted from a chat, so line endings may differ. I'll note it as "size differs by 1 byte from the archived transcription (line-ending/trailing-newline), content values identical" — but careful, the values differ (established=4 vs 3). Let me not overclaim: I'll just record the observed size 751 B.
    event_time_end=1893499200 → date? 1893499200 / 86400 = 21915.5 days... Let me compute: 1893456000 = 2030-01-01T00:00:00Z. So 1893499200 = 1893456000 + 43200 = 2030-01-01 12:00:00 UTC. The docs say 2030-01-01. Let me verify with python.

Let me run these computations now to be accurate.

All calculations verified:

    e8 85 71 8f ff is exactly call 0x1400af690 from VA 0x1407b8506 ✓ (independently recomputed)
    V9 is length-preserving (5→5) ✓
    DAILY_GATE bp is 0x1000 before the patched je ✓
    ASLR base 0x7FF7B6EE0000 consistent across all 6 breakpoints ✓
    Old Continue bps: +9E4970, +9E5500, +9E678B ✓
    je 74 0d at VA 0x140667146 targets 0x140667155 — matches the disassembly je 0x140667155 ✓
    jne 75 05 at VA 0x1407b846b targets 0x1407b8472
    event_time_end 1893499200 = 2030-01-01 12:00:00 UTC (docs say 2030-01-01 ✓, and note the +12h)
    2147310847 = 2038-01-17 03:14:07 (the "safe ceiling"), 2147483647 = 2038-01-19 03:14:07 (INT_MAX). Docs say "hard wall 2038-01-17/19" ✓
    key/id=-2128831035 = FNV-1a 32-bit offset basis as signed ✓ (already noted in LATEST_LOGS_ANALYSIS)

Now let me write Part 5.
WriteALL-MY-LOGS-SO-FAR/01_research_archives/CK2_MJ_RESEARCH_ARCHIVE_PART5.md+521

Now the dissection report for last log/.
WriteALL-MY-LOGS-SO-FAR/00_START_HERE/DISSECTION_REPORT_2026-08-30.md+106

Now append §11 to RAWLOG_NETNEW_EXTRACTS.md. Let me check the end of that file first.

I'll insert §11 before "## How this was verified". Let me use edit_file.

Wait — I need to verify the §11.5 claim about je at raw 0x007856E8 and the "RestoreDeviceObjects" naming. Let me not over-claim. In session memory: "Restore gate (net-new): 0x1407862e1 call IsActiveForPlaythrough → test al,al → 0x1407862e8 je 0x1407862f7 (raw 0x007856e8, still 74 0d in V8) → 0x1407862f2 call UpdateFeatProgress." Good, that matches. I dropped the "RestoreDeviceObjects" name, which is fine — but I should double-check whether the function name was mentioned. Session memory says "the unpatched RestoreDeviceObjects gate (raw 0x007856E8)". Hmm, it says the function is RestoreDeviceObjects. Let me check the raw log for that to be accurate.

Let me grep the raw log.
Edit+115−3

Great — this is very valuable. Several corrections to my Part 5:

    The "restore gate" function is at raw 0x7855c0 (VA 0x1407861c0), and it also has a global-flag bypass: cmp byte ptr [rip+0xf5ce84], 0; jne 0x1407862ea — i.e. if that global is nonzero it calls UpdateFeatProgress unconditionally. That's net-new detail.

    The function name RestoreDeviceObjects was the earlier session's guess (CInGameIdler::RestoreDeviceObjects from a Linux-symbol match attempt), and the later session (last log (for now).txt) treats it as "the RESTORE function (raw 0x7855c0)" — possibly RestoreGameState/RestoreFeats, unidentified. So I should be careful: the name is a candidate, not confirmed.

    Important: CALC_RETURN_PATH al=0 and the trace also showed UPDATE_ENTRY fired on cold. Good — that matches my Part 5.

    Also note in another raw log.txt line 2207: "Restore gate false and no update entry: the unpatched 0x007856e8 gate is the direct cold-load blocker." — that was the pre-trace hypothesis, later disproven by the trace (RESTORE_GATE al=1). Hmm wait — session memory says "RESTORE_GATE al=1 in both". Let me verify against the actual trace log. Let me grep x64dbg logs.txt for RESTORE_GATE and al=.

Let me check the actual trace content to be precise.

This is the definitive trace. Important corrections to my Part 5 draft:

    DAILY_GATE rip=00007FF7B7546146 → offset 0x7FF7B7546146 - 0x7FF7B6EE0000 = 0x666146 ✓ (matches my §C5 note).
    UPDATE_ENTRY rip=00007FF7B7698E60 → 0x7B8E60 = UpdateFeatProgress ✓
    RULER_INFO_CHECK rip=00007FF7B769848D → 0x7B848D ✓, rax=00000222A2B27050 zf=0 (pointer non-null, so the check passed)
    CALC_RETURN_PATH rip=00007FF7B7698519 → 0x7B8519 ✓
    CALC_RESULT rip=00007FF7B7698E86 → 0x7B8E86 ✓
    RESTORE_GATE rip=00007FF7B76662E8 → offset 0x7862E8... wait: 0x7FF7B76662E8 - 0x7FF7B6EE0000 = 0x7862E8. But raw = VA - 0x140000C00 → 0x1407862E8 - 0x140000C00 = 0x7856E8 ✓. Good.
    RESTORE_GATE al=1 in both bursts ✓ (so the restore gate passed on cold too!) — that means the earlier hypothesis at line 2207 ("Restore gate false and no update entry") was disproven.

Hmm — but careful. The bplog prints al at the breakpoint address, which is the je at raw 0x7856e8, right after test al,al. al=1 means IsActiveForPlaythrough returned 1 → the je is NOT taken → UpdateFeatProgress IS called. And indeed UPDATE_ENTRY follows. So IsActiveForPlaythrough returned 1 on the cold load!

That's a huge detail: it means V8's bypass of IsActiveForPlaythrough inside CalcShouldTrack wasn't even needed — the function returned true. Wait, but the trace was on V8, so the bypass was active... no, the bypass was in CalcShouldTrack at raw 0x7b786b, not at the caller sites. IsActiveForPlaythrough itself (0x1407B8370) was unpatched, and RESTORE_GATE logs its result (al after test al,al), showing al=1 → it returned TRUE.

So on cold load:

    RESTORE path: IsActiveForPlaythrough = 1 → UpdateFeatProgress called → CalcShouldTrack returns 0.
    DAILY path: DAILY_GATE al=80 zf=0 at raw 0x665546 (mid-instruction, al is meaningless there — it's rax low byte of some pointer). Then UPDATE_ENTRY.

So the whole failure is CalcShouldTrackFeatProgress returning 0 while every logged checkpoint passes: ruler-info non-null (zf=0), and it reaches CALC_RETURN_PATH (raw 0x7b790b... wait, CALC_RETURN_PATH bp is at raw 0x7b7919? Let's compute: 0x7B8519 - 0xC00 = 0x7B7919. Yes — the bp is at VA 0x1407B8519 = raw 0x7B7919, which is right after setne al at 0x7B790B (5-byte call ends at 0x7B790B, setne al is 2 bytes → 0x7B790B-0x7B790C, then ret). Hmm, actually the log line at 997 said "disasm5 shows je 0x1407862f7 at raw 0x7856e8". And CALC_RETURN_PATH at VA 0x1407B8519 = raw 0x7B7919. The call is at 0x7B7906 (5 bytes → ends 0x7B790B), setne al at 0x7B790B (2 bytes: 0F 95 C0) → ends 0x7B790D. So 0x7B7919 is a bit further. Whatever — the important part: al=0 at CALC_RETURN_PATH means the gate returned 0 and setne al set al=0.

Wait, setne al after test al,al... Actually in my Part 5 I wrote "call 0x1400AF690 → setne al → ret". With setne, al = (ZF==0) i.e. al = (gate_result != 0). So al=0 means the gate returned 0. ✓ Consistent.

So the conclusion is solid: 0x1400AF690 returned 0 on cold load, while on warm it returned 1. That's exactly what V9 forces.

Also important: IsActiveForPlaythrough returned 1 on cold load (RESTORE_GATE al=1). This is a significant net-new fact that partly undercuts V8's premise at the caller level. Actually V8 patched the result usage inside CalcShouldTrack at 0x7b786b (jne→jmp). Hmm, but if IsActiveForPlaythrough returned 1 in the RESTORE caller, maybe it also returned 1 inside CalcShouldTrack. The trace doesn't log CalcShouldTrack's own call result (0x7b7864 → test at 0x7b7869). But RESTORE_GATE al=1 shows the function can return 1 during a cold load.

Let me be careful and precise. I'll record: "RESTORE_GATE logged al=1 on both bursts, i.e. IsActiveForPlaythrough returned TRUE during the cold load at the restore caller — so at least at that site the playthrough was recognised, and V8's premise (that this gate was the blocker) was doubly wrong."

Hmm, but I should double check: is RESTORE_GATE's al really the result of IsActiveForPlaythrough? The bp is at raw 0x7856e8 which is the je. Before it: call IsActiveForPlaythrough at 0x7856e1 (5 bytes → 0x7856e6), test al,al at 0x7856e6 (2 bytes → 0x7856e8), je at 0x7856e8. Yes. So al at 0x7856e8 = result of IsActiveForPlaythrough. ✓

Also tf=1 (trap flag) is set on DAILY_GATE and RESTORE_GATE — that's a normal artifact of breakpoint single-step restoration.

Also: DAILY_GATE al=A0 / al=80 — meaningless (mid-instruction), confirming it's not a test al,al site. This strengthens my §C5 correction.

Now, also note: RULER_INFO_CHECK rax=00000222A2B27050 zf=0 — same pointer in all four bursts, warm and cold. So the ruler-info pointer is identical warm and cold. Good detail.

Let me now fix Part 5 §C1/§B and the §11.5 section with these precise facts. Also I need to correct my claim in Part 5 §B2 row 6 ("RestoreDeviceObjects gate... trace showed RESTORE_GATE al=1") — that's right, but I should add the stronger statement that IsActiveForPlaythrough returned true.

Also the restore function has an extra global-flag bypass: cmp byte ptr [rip+0xf5ce84], 0; jne 0x1407862ea. And the restore function starts at raw 0x7855c0 (VA 0x1407861C0).

Let me also double check whether the another raw log.txt line 2207 hypothesis was later disproven — yes by the trace.

Now let me make these edits.

Also: I claimed in Part 5 §C1 that 0x1407862e8 "still 74 0D in V8 and V9" — confirmed by another raw log.txt:455. Good.

Let me also verify RedKing.csv FEAT keys quickly since I reference them in the upcoming FEAT_CACHE doc. And check monarchs.txt established levels. Actually those are in session memory as verified. But per the "check artifacts" instruction, let me re-verify the key facts I'm about to write into FEAT_CACHE_PEAK_TIER_ICON.md.

Let me do the edits first, then verify CSV/monarchs.

Trace verified line by line — several facts are sharper than my draft. Fixing Part 5 first:

Now fix §B2 row 6 and §C1 (add the restore function detail) and §C5.

Wait — I need to be careful about ordering. Row 6 is now ❌ refuted; but row 7 says "The final eligibility gate 0x1400AF690 returns 0" — confirmed. Fine.

Now update §C1 to add the restore function detail and the trace's implication about IsActiveForPlaythrough.

Now update §C5 with the DAILY_GATE row addition re al=80/A0 being meaningless. Let me refine that row.

Also, I should verify the claim about mov qword ptr [rbp+0x648], rdi at raw 0x665545. Let me check the log for the disassembly at raw 0x665546.
Edit+42−12×3

Good, all confirmed. The log itself reached the same conclusion. Let me check what's actually at raw 0x665546 in the log (line ~226 onwards).

Now let me see what the disassembly actually found at raw 0x665546 (the resolution).

Excellent — my §C5 note is confirmed and there's more nuance: the DAILY_GATE bp at raw 0x665546 sits mid-instruction (48 89 BD 48 06 00 00 = mov qword ptr [rbp+0x648], rdi at raw 0x665545, VA 0x140666145), inside a different, larger function (region A, VA ~0x140665EF8) that is NOT the documented DailyUpdate. And the xref scan confirmed region A does NOT call UpdateFeatProgress directly — so DAILY_GATE firing tells us nothing about which caller ran. The actual caller must be determined by the UPDATE_ENTRY ordering (RESTORE_GATE fired after DAILY_GATE→UPDATE_ENTRY in the first burst... actually the first burst has no RESTORE_GATE before UPDATE_ENTRY, so the caller is unknown/region B daily or something else).

Hmm, careful. Burst 1 (lines 373–377): DAILY_GATE → UPDATE_ENTRY → RULER_INFO_CHECK → CALC_RETURN_PATH → CALC_RESULT. No RESTORE_GATE. Burst 2 (381–385): RESTORE_GATE → UPDATE_ENTRY → … So there were two distinct update entries: one preceded only by DAILY_GATE, one by RESTORE_GATE.

So in burst 1 the caller of UpdateFeatProgress is unidentified (the DAILY_GATE bp is misplaced). Good nuance. Let me record that honestly.

Let me now fix §C5's DAILY_GATE row to be precise, and add a note in §C1/B1.

Let me update the §C5 row.

Now update §11.6 in RAWLOG_NETNEW_EXTRACTS.md with the same precision, and add a §11.8 about the region A / DAILY_GATE function.

Actually §11.6 already has the DAILY_GATE bullet. Let me refine it.
Edit+1−1

Now I need to write: 4. 03_analysis/V9_RUNTIME_RESULTS.md and 03_analysis/FEAT_CACHE_PEAK_TIER_ICON.md 5. 04_test_guides_and_reports/CK2_MJ_V9_TEST_GUIDE.md 6. Reconstruct RUN_APPLY_CK2_MJ_V9_INLINE.ps1 7. Living-doc repairs 8. CONTRADICTIONS.md §12/§13/§14

Let me first verify the RedKing.csv feat keys and monarchs.txt thresholds, since those go into the FEAT_CACHE doc. And verify the notification quote in V6_RUNTIME_RESULTS.md.
Edit+18−3

The CSV is likely comma-separated with semicolon in the text? Let me look at actual lines.

All verified verbatim:

    RULER_FEAT_LEVEL_1_NAME;Bronze (line 683) — the tier name
    FEAT_COMPLETE_LOG (690) — "Word has spread far and wide of your achievements. You completed the challenge "$FEAT$"."
    FEAT_LEVEL_1_COMPLETE_LOG (691) — "…You earned the rank of $LEVEL$ in the "$FEAT$" challenge."
    FEAT_LEVEL_1_COMPLETE_SETUP (720) — "Challenge Rank Bronze Earned" ← the notification title
    FEAT_HIGHSCORE_TT (724) — "Best Result: $HIGH_SCORE|G$"
    FEAT_CURRENT_TT (729) — "Current Progress: $SCORE|Y$"

Let me also get FEAT_LEVEL_2_NAME / 3_NAME and FEAT_TEXT, and check the notification quote in V6_RUNTIME_RESULTS.md. And verify monarchs.txt established levels.

All confirmed:

    established = { icon = GFX_dream_home_34, levels = { 4 6 8 }, update = { set_variable = { which = global_established value = 0 } ... } }
    RULER_FEAT_established_NAME;Established, RULER_FEAT_established_PROGRESS;Number of Landed Dynasty Members
    Tier names: Bronze / Silver / Gold (RULER_FEAT_LEVEL_1/2/3_NAME)
    FEAT_TEXT;Challenges!
    FEAT_PROGRESS_TT;Progress $FEAT_PROGRESS|Y$ / $FEAT_LEVEL_VALUE$

Note: this monarchs.txt entry is "Paul I Subic" with dynasty = 18005 # Subic and c_470001 # Paul. Interesting — char #470001 matches the game.log line "Deleting playerdata for char #470001". Good cross-reference.

Note also: levels = { 4 6 8 } for established and levels = { 4 } for conquerer_from_bribir.

Hmm — session memory said thresholds established levels = { 4 6 8 } via global_established. ✓ And the Bronze tier = level 1 = 4 landed dynasty members.

Now let me look at the V6_RUNTIME_RESULTS.md line 43 context to quote the notification.

The verbatim game.log notification is confirmed: You earned the rank of Bronze in the "Established" challenge. — matches FEAT_LEVEL_1_COMPLETE_LOG template exactly.

Also note: Bronzeman_pavao_croatia.ck2 has global_established = 2.000 and internal date 1278.1.1 — this is exactly the day-one save that showed Bronze. Confirmed independently in V6_RUNTIME_RESULTS.md (different session, different measurement) — excellent corroboration.

Now let me write the two analysis docs.

First, V9_RUNTIME_RESULTS.md. Let me check the format of V6/V7 runtime results docs for consistency.

Note there is no V8_RUNTIME_RESULTS.md. I'll write V8_AND_V9_RUNTIME_RESULTS.md covering both — actually the plan said V9_RUNTIME_RESULTS.md. I'll name it V9_RUNTIME_RESULTS.md and include the V8 disproof section inside it (matching the V6/V7 naming pattern).

Now write it.
WriteALL-MY-LOGS-SO-FAR/03_analysis/V9_RUNTIME_RESULTS.md+222

Now the FEAT_CACHE_PEAK_TIER_ICON.md doc — the answer to the user's question.

Let me double check one thing: does the cache apply to every save? Yes — the cache file is cache/q847rsja8ndx keyed by user_id, with no save identifier. And the save has global_established current value. The medal icon reads from... which? The user observed Bronze on a day-one save whose global_established=2.000. So the icon comes from the cache (peak = 4 = Bronze), not from the save.

Hmm, but careful — is that certain? Alternative explanation: the MJ tab on cold start reads the cache. Let me reason about what evidence exists:

    Bronzeman_pavao_croatia.ck2 internal date 1278.1.1, global_established = 2.000.
    Cache peak established = 4.
    User: "i loaded first day save, had already bronze (highest what i got) score."
    Bronze = level 1 = 4 landed dynasty members.
    2 < 4 → the save's own value cannot produce Bronze. ⇒ The icon must come from the cache. Solid.

And FEAT_HIGHSCORE_TT;Best Result: $HIGH_SCORE|G$ vs FEAT_CURRENT_TT;Current Progress: $SCORE|Y$ proves the UI tracks both separately — the cache feeds "Best Result", the save feeds "Current Progress".

And the notification: FEAT_LEVEL_1_COMPLETE_LOG fires when a level is newly granted. Since Bronze is already recorded as achieved (cached peak ≥ 4), no new level is granted on that save, so no popup.

The user's hypothesis "the cache file applies to every save unless deleted" — confirmed. Reset = back up + rename/delete the cache file. Never wipe_feats.

Also: restarting the campaign — same thing, cache persists.

One more nuance: SetCachedProgressIfHigher(CString const&, int) — "if higher" semantics = peak. And CheckNeedsCache.

Let me also verify the exact cache path & filename and the feat_progress_storage naming. Let me grep the raw logs for the verbatim cache dump to quote accurately.

Important — line 121 of another raw log.txt gives a different, later explanation for the V8 main-menu zeroing: "Forcing UpdateFeatProgress() to calculate feats while the featured ruler key was NULL caused the engine to evaluate challenges for a non-existent ruler. That calculated 0 progress for all 33 challenges and pushed those 0s into the local feat cache (cache/q847rsja8ndx), wiping out the main-menu display!"

Hmm — that contradicts what I wrote in V9_RUNTIME_RESULTS §2 point 3 ("What V8 did not do: it did not erase the disk cache"). Let me check: session memory said "User: 'it doesn't change, since it's not updating with new feats' (about the cache across a confirmed-V8 cold load) → V8 did not erase the disk cache; the zero display is an in-memory/UI condition."

But this line 121 is a diagnosis written in the chat, and it may have been superseded by the later evidence (cache still showed established=3, user_id=84696387 after the failed V8 run). Let me check the sequence carefully.

Line 121 is in another raw log.txt — that's session 5 (arena/01a04d46). It's a summary of the previous session's diagnosis. Then lines 181-294 show the user pasting the cache. Let me look at what the cache actually showed at that point — did it show established=0 or established=3?

Let me read lines 100-300 of another raw log.txt.

Very important correction. The user's pasted cache shows:

key=-2128831035
id=-2128831035
user_id=1179784490
not_so_land_locked=0
heretical_company=0
independence_day=0
not_so_little_armenia=0
keep_your_friends_close=0
we_will_not_submit=0
category=-1991027533

and then (per the agent's reading, "lines 16 and 17") conquerer_from_bribir=1 and established=4.

So the cache was NOT wiped — it still showed established=4 after the failed V8 run. So the chat's claim in point 2 ("pushed those 0s into the local feat cache, wiping out the main-menu display") is contradicted by the very same paste — the cache still had established=4. The agent itself then corrected to "pushed those 0s to the display" (line ~283). Good — so the display-only explanation is the surviving one, and the "wiped the cache" claim is a dead end.

Also important: the "special_event NULL" theory (theory #5) was the diagnosis at the time, and it's the one that the trace later showed was NOT the operative cause (ruler-info check passed). But it remains the chat's stated explanation for why V8 broke the display. I should record it as "stated explanation, later weakened by the trace" — and note the conflict.

Also this gives me the exact verbatim cache field order for user_id=1179784490: key, id, user_id, not_so_land_locked, heretical_company, independence_day, not_so_little_armenia, keep_your_friends_close, we_will_not_submit, …, category. Good.

Now I must fix V9_RUNTIME_RESULTS §2 to be accurate. Let me correct.

Also I should double check: is established=4 at that point? The agent says "Look closely at lines 16 and 17 of the file content you pasted: conquerer_from_bribir=1, established=4". Yes.

Let me fix §2 of V9_RUNTIME_RESULTS.md.

Now write FEAT_CACHE_PEAK_TIER_ICON.md.

Let me double-check the exact cache field list and the "32 feats" count. Also, let me check 13_save_and_cache/q847rsja8ndx.txt for the full verbatim structure so I can quote accurately.
Edit+19−3

The verbatim cache structure: 33 feat keys (let me count): not_so_land_locked, heretical_company, independence_day, not_so_little_armenia, keep_your_friends_close, we_will_not_submit, a_ruler_harder_than_steel, hide_the_pain_harold, rule_with_an_iron_fist, long_live_the_queen, gods_will_be_done, convert_hindu_to_muslim, conquerer_from_bribir, established, subic_stantial_legacy, heiroglyphics, tutan_kha_doom, dont_stop_me_now, french_toast, his_unfulfilled_dream, why_dont_you_love_me, moving_up, lovely_rule, kon_once_kon_twice, one_of_us, peace, secret_stays_with_me, dragons_fire, princes_of_wales, love_spoons, time_bending, gloves_come_off, preemtive_selfdefense = 33 feats. ✓ matches "33 challenges".

Now write FEAT_CACHE_PEAK_TIER_ICON.md.
WriteALL-MY-LOGS-SO-FAR/03_analysis/FEAT_CACHE_PEAK_TIER_ICON.md+208

Now: reconstruct RUN_APPLY_CK2_MJ_V9_INLINE.ps1 verbatim from last log (for now).txt lines ~6280-6360.

Let me find it.

There are two occurrences: 6206-6257 and 6297-6353. Let me diff them and pick the final one (the one at 6297 is the one "saved" per line 6367).

Let me print 6290-6360.

I have the verbatim text. Let me diff the two copies (6206-6257 vs 6297-6353) to make sure they're identical.

The two copies are identical apart from blank-line spacing (the second has blank lines between blocks — that's the "final formatted" version). Both end the same way. I'll use the second (6297–6353) which is the formatted, final version.

Now write the PS1 file verbatim. Note the log lines end with \r\n. I'll write with LF (repo convention — let me check existing ps1 files' line endings).

Let me check line endings of patch_ck2_mj_v9.ps1 (CRLF or LF).

LF, no BOM. Good — I'll write LF.

Note the v9.ps1 header gives an even better description of what 0x1400af690 does: "the 'all linked feature entries visible/available' eligibility check, which walks the global feature list through 0x14072c6c0/0x14072d010 and ANDs each entry's +0x58/+0x59 flag byte". That's a net-new detail I should include. Let me update V9_RUNTIME_RESULTS §4 with that.

Now write the inline ps1.
WriteALL-MY-LOGS-SO-FAR/05_patches_and_scripts/ps1/RUN_APPLY_CK2_MJ_V9_INLINE.ps1+90

Now verify the reconstructed script's PowerShell syntax parses and that the byte logic is right. Is pwsh available? Probably not. Let me check. If not, I can at least verify the logic by simulating in Python against the stock exe... but the stock exe is in the repo (10_binary_artifacts/executables/windows/CK2game333.exe). I could verify that the pre-check bytes match stock at those offsets — that would be a genuine verification of the patch logic.

Let me check if pwsh exists, and verify the offsets in the stock exe.

No PowerShell available. I'll verify the byte logic with Python against the stock exe — this actually executes the same logic (the offsets and byte values) that the PS1 encodes.

Let me:

    Confirm stock CK2game333.exe SHA = 656f4f48…
    Confirm stock bytes at 0x666546 = 74 0d, 0x7b786b = 75 05, 0x7b7906 = e8 85 71 8f ff.
    Apply all V8+V9 edits in Python and check the resulting hash equals 61e4345b… — wait, that would only work if stock → V9 requires exactly these edits. But V9 = stock + V2..V9 edits (many more). The inline script accepts V7 or V8, not stock. So I can't reproduce V9 from stock with only 3 edits.

But I CAN verify: stock has the expected "before" bytes at all three sites (so the pre-checks are correct for V7/V8, which differ from stock only at other offsets). And I can verify the V9 patcher's own logic if build_v9_chain.py is runnable.

Actually — better: 05_patches_and_scripts/py/build_v9_chain.py exists. Let me check if it's runnable here (needs the stock exe + the patcher sources). That's the project's own tool and it computes the V9 hash. Running it would be the real check.

Let me look at it.

This script is runnable from the repo root. Let me run it — this is the project's own verification tool and it asserts every intermediate hash. Running it is the real check for the V9 hash claim.

The project's own chain replay runs clean and produces 61e4345ba1395f09d26f84bf030ae0474fce3f0635a3516edea56b46c486d687 — the V9 hash I've been asserting. Every intermediate assert passed (the script uses assert for pre-checks, so it would have raised).

Now let me verify the inline PS1's pre-check bytes against the actual V7 and V8 images that the chain produces. I'll replicate in Python: build V7 and V8 from the chain, then check that:

    V7 has 74 0d at 0x666546, 75 05 at 0x7b786b, e8 85 71 8f ff at 0x7b7906
    V8 has 90 90 at 0x666546, eb 05 at 0x7b786b, e8 85 71 8f ff at 0x7b7906
    Applying the PS1's logic to V7 and to V8 both produce 61e4345b…

That's a genuine execution of the reconstructed script's logic. Let me write a harness that parses the PS1 file's offsets/bytes rather than hard-coding them, so it tests the shipped file.

Actually simpler and more honest: parse the PS1 file for the byte assignments and pre-checks, then apply. Let me do a focused harness that extracts the offsets and target bytes directly from the .ps1 text.

Hmm, parsing PS1 with regex is doable but fiddly. A cleaner approach: extract from the PS1 the three sites and their expected old/new bytes via regex on the specific patterns, then simulate. Let me do it.

Patterns in the file:

    if ($d[0x00666546] -ne 0x74 -or $d[0x00666547] -ne 0x0d) → site 0x666546, old = 74 0d
    $d[0x00666546] = 0x90; $d[0x00666547] = 0x90 → new = 90 90
    if ($d[0x007b786b] -ne 0x75 -or $d[0x007b786c] -ne 0x05) → old = 75 05
    $d[0x007b786b] = 0xeb; $d[0x007b786c] = 0x05 → new = eb 05
    if ($d[0x007b7906] -ne 0xe8 -or ...) → old = e8 85 71 8f ff
    $d[0x007b7906] = 0xb0; ... = 0x01; ... = 0x90 x3 → new = b0 01 90 90 90

Let me write a parser that pulls the pre-check old bytes and the assignment new bytes generically.

The project's own chain replay ran clean and reproduced the V9 hash. Now let me verify the reconstructed PS1's logic actually executes correctly against real V7/V8 images — parsing the shipped file rather than hard-coding:

The parser extracted everything correctly and all three declared hashes are present in the replayed chain. Now let me actually simulate the PS1's patch logic on the real V7 and V8 images and confirm both produce V9.

Let me extend the harness.

The reconstructed PS1's logic is verified: feeding it a real V7 image produces exactly the declared V9 hash, and feeding it a real V8 image also produces exactly the declared V9 hash. The pre-check bytes are exactly what's in the real images.

Now let me write the V9 test guide, then do the living-doc repairs.

Let me look at an existing test guide format quickly.

Both branches of the reconstructed script produce the exact V9 hash. Now the test guide:

Now let me write the V9 test guide. I should also add a warning banner to the V8 test guide noting V8's premise was disproven. Let me first write the V9 guide.
WriteALL-MY-LOGS-SO-FAR/04_test_guides_and_reports/CK2_MJ_V9_TEST_GUIDE.md+156

Now let me verify that the bat files referenced actually exist: CHECK_CK2_MJ_V9.bat, APPLY_CK2_MJ_V9.bat, REVERT_CK2_MJ_V8_TO_V7.bat. And MJ_V9_CLEAN_TRACE.txt has a V9_GATE_FORCE breakpoint.

All confirmed:

    APPLY_CK2_MJ_V9.bat, CHECK_CK2_MJ_V9.bat, REVERT_CK2_MJ_V9_TO_V8.bat, REVERT_CK2_MJ_V8_TO_V7.bat all exist ✓
    MJ_V9_CLEAN_TRACE.txt has V9_GATE_FORCE at CK2game.exe+7b8506 ✓ and still has DAILY_GATE at +666146 ✓ (typo confirmed present)

Note: my guide says "Revert ... returns you to V8. ... or run REVERT_CK2_MJ_V8_TO_V7.bat" — both exist ✓.

Now, living-doc repairs. Let me start with CONTRADICTIONS.md §12–§14.

Note: session memory said "CONTRADICTIONS.md (11 entries)". So §1–§11. Good. Now append §12, §13, §14.

Let me check the existing entry about identity drift being refuted so I reference it correctly.

The "identity drift refuted" claim is in FEAT_RESET_QUIT_VS_RESIGN.md, not in CONTRADICTIONS.md. So §12 should be a new entry that records the conflict with FEAT_RESET_QUIT_VS_RESIGN.md.

Let me append §12–§14 to CONTRADICTIONS.md.

14 sections now. Good.

Now: add the addendum to V9_COLD_LOAD_FEATS_FIX.md, STATUS.md, CASES_AND_FINDINGS.md, MASTER_ARTIFACT_TABLE.md, PLAN.md, README.md, ORGANIZATION_HISTORY.md, 12_raw_chat_logs/INDEX.md, handoff banner, WINDOWS_333_PATCH_MAP.md V8/V9 rows.

Let me look at V9_COLD_LOAD_FEATS_FIX.md first.

Continue with the living-doc repairs. Next: append the addendum to V9_COLD_LOAD_FEATS_FIX.md.

Note: this doc says CALC_RETURN_PATH is at raw 0x7b7919. Let me verify: VA 0x1407b8519 - 0x140000c00 = 0x7b7919. ✓ And the trace rip 00007FF7B7698519 - 0x7FF7B6EE0000 = 0x7b8519, so VA = 0x140000000 + 0x7b8519 = 0x1407b8519 ✓, raw = 0x7b7919 ✓. Consistent.

Now append the addendum.

Oops — I wrote "raw 0x7b7c60-site" which is a typo I invented. Let me fix that: UPDATE_ENTRY is at CK2game.exe+7b8e60 → VA 0x1407b8e60 → raw 0x7b8260. Let me correct.

Now update WINDOWS_333_PATCH_MAP.md with V8/V9 rows. Let me check it.
Edit+6−5

Interesting — the V4 "In-game feat tracking gate" at 0x007b78eb 75 0c → eb 0c! That's inside CalcShouldTrackFeatProgress (raw 0x7b7850 + 0x9b = 0x7b78eb). Wait, that's the region of the IsActiveForPlaythrough result... Actually raw 0x7b786b is the V8 site (75 05 → eb 05). And 0x7b78eb is a different gate (75 0c → eb 0c), already patched in V4!

Hmm — and the eligibility tail at 0x000aeb83 (24 bytes) — that's raw 0xaeb83, which is inside 0x1400af690 (raw 0xaea90)! Indeed: 0xaea90 + 0xf3 = 0xaeb83. So V4 ALREADY patched the tail of the final eligibility gate!

That's a significant net-new insight: the V9 site (0x007b7906 → call 0x1400af690) and the V4 eligibility-tail patch (0x000aeb83, 24 bytes inside 0x1400af690) are in the same function. V4 patched the tail of 0x1400af690 to compute save_ok && !ruler_designer && (stock_checksum || !steam_active), and V9 bypasses the call to it entirely.

Wait — but careful. 0x1400af690 is raw 0xaea90. And 0x1400af050 is raw 0xae450 (the singleton). The eligibility tail at raw 0xaeb83 is inside 0xaea90's function. And the V9 doc says 0x1400af690 "walks the global feature list through 0x14072c6c0/0x14072d010 and ANDs each entry's +0x58/+0x59 flag byte". Hmm, and patch_ck2_mj_v9.ps1 says "the 'all linked feature entries visible/available' eligibility check".

So: the final gate at raw 0x7b7906 calls 0x1400af690 whose body was already partly rewritten by V4 (raw 0xaeb83 tail). The V4 rewrite computes save_ok && !ruler_designer && (stock_checksum || !steam_active) — hmm, and the stock version of the tail was cmp byte ptr [rdi+0x61],0; je …; cmp byte ptr [rdi+0x63],0; je …; cmp byte ptr [rdi+0x62],0; je …. The V4 patch turned that into checking +0x61 and +0x63 and +0x65 (with ff c0 = inc eax).

Interesting: the V4 patch tail is 31 c0 66 83 7f 61 01 75 0f 80 7f 63 00 75 06 80 7f 65 00 75 03 ff c0 90:

    31 c0 xor eax,eax
    66 83 7f 61 01 cmp word ptr [rdi+0x61], 1
    75 0f jne +0x0f → (to the ret with eax=0)
    80 7f 63 00 cmp byte ptr [rdi+0x63], 0
    75 06 jne +6
    80 7f 65 00 cmp byte ptr [rdi+0x65], 0
    75 03 jne +3
    ff c0 inc eax
    90 nop

So V4 made 0x1400af690's tail return 1 iff [+0x61]==1 && ([+0x63]!=0 || [+0x65]!=0).

That is exactly the +0x61/+0x63 logic I described in Part 5 §C2 ("true iff [this+0x61]!=0 and [this+0x63]!=0") — but actually the V4-patched version is [+0x61]==1 && (+0x63 || +0x65). My §C2 description came from the V9 session's disassembly of... hmm, which version? The V9 session disassembled the STOCK exe. So the stock tail is different from the V4-patched tail.

This is important and I should be careful not to assert something wrong. Let me re-read what I wrote in Part 5 §C2:

"- Otherwise true iff [this+0x61]!=0 and [this+0x63]!=0. +0x62 is irrelevant here."

Hmm. That's the V9 session's reading. But the V4 patch map says the stock tail was cmp [rdi+0x61],0 / je / cmp [rdi+0x63],0 / je / cmp [rdi+0x62],0 / je / xor esi,esi / movzx eax,sil — i.e. stock returns 0 if any of +0x61/+0x63/+0x62 is 0... wait je jumps when equal to 0. Stock: 80 7f 61 00 cmp byte [rdi+0x61],0; 74 0c je +0xc; 80 7f 63 00 cmp [rdi+0x63],0; 74 06 je +6; 80 7f 62 00 cmp [rdi+0x62],0; 74 02 je +2; 33 f6 xor esi,esi; 40 0f b6 c6 movzx eax,sil.

Hmm, that reads: if +0x61==0 → skip (jump +0xc); if +0x63==0 → jump +6; if +0x62==0 → jump +2; then xor esi,esi; movzx eax,sil → eax=0. Hmm, that gives eax=0 in the fall-through. So the jumps go to something that sets eax=1. It's a "return true if any of these is set" pattern, roughly.

This is getting deep and I risk asserting something wrong. The honest thing: my Part 5 §C2 says "true iff [+0x61]!=0 and [+0x63]!=0" — I should verify this against the actual stock bytes rather than trusting the summary. But the V4 patch map is authoritative for the stock bytes at raw 0xaeb83.

Rather than re-derive the whole function, the right move is to flag the interaction: V4 already rewrote the tail of the very function that V9 bypasses. That's a genuinely important, previously unrecorded connection, and it's verifiable from the patch map alone (raw 0xaeb83 is inside raw 0xaea90's function).

Let me verify: is 0xaeb83 within the function starting at 0xaea90? Function starts at raw 0xaea90. 0xaeb83 - 0xaea90 = 0xF3 = 243 bytes into the function. Plausible for a function of that complexity. And patch_ck2_mj_v9.ps1's header says 0x1400af690 "walks the global feature list through 0x14072c6c0/0x14072d010 and ANDs each entry's +0x58/+0x59 flag byte" — and the V4 patch map says the tail computes save_ok && !ruler_designer && (stock_checksum || !steam_active). Those are two different descriptions of the same function's tail. Both come from repo docs.

Hmm, wait. Let me re-read the V4 map: "| 0x000aeb83 (24 bytes) | — | see below | see below | Eligibility helper tail |" and "Effective: save_ok && !ruler_designer && (stock_checksum || !steam_active)". The names save_ok, ruler_designer, stock_checksum, steam_active map to [+0x61], [+0x63], [+0x65]... Actually with the V4 patch: [+0x61]==1 && ([+0x63]!=0 || [+0x65]!=0). So save_ok = +0x61, and the OR pair = +0x63/+0x65.

OK. So the V4 rewrite made the eligibility helper's tail depend on +0x61, +0x63, +0x65 — the same singleton flags. And the V9 trace showed CALC_FAIL_PATH (which tests +0x60/+0x65) never fired, and CALC_RETURN_PATH al=0 → the helper returned 0 → with V4's tail that means [+0x61]!=1 OR ([+0x63]==0 AND [+0x65]==0).

Hmm! But wait — is that right? There's a subtlety: the helper's early part (the 0x14072D540 eligibility loop) can also return 0 before reaching the tail. The V9 session's reading says "With dl=1: call the eligibility loop 0x14072D540 ... If it returns 0 → the gate returns 0". So there are two possible cold-failure points inside the helper.

I should not over-assert. Let me state what's verifiable and flag the rest as a refinement of §C2.

Let me verify the stock bytes at raw 0xaeb83 directly from the repo's stock exe — that's a real check I can run.

Let me do that.

Before touching the patch map, let me verify something the ingest surfaced — whether V4 already patched inside the very function V9 bypasses:

Confirmed, and it's a significant net-new finding:

    The V9 site at raw 0x007b7906 calls 0x1400af690 (computed from the stock bytes: e8 85 71 8f ff → rel32 0xff8f7185 → target 0x1400af690). ✓
    The V4 "eligibility helper tail" patch is at raw 0x000aeb83 = VA 0x1400af783, which is 0xF3 bytes inside the function that starts at VA 0x1400af690 — i.e. the same function V9 bypasses.
    The stock bytes there are exactly 80 7f 61 00 74 0c 80 7f 63 00 74 06 80 7f 62 00 74 02 33 f6 40 0f b6 c6, matching the patch map verbatim. ✓

So: V4 already rewrote the tail of 0x1400af690 (from "any of +0x61/+0x63/+0x62 set" to "+0x61==1 and (+0x63 or +0x65)"), and V9 then bypasses the call to that function entirely. This is a real, previously unrecorded interaction — and it explains a lot: the V9 gate is not virgin stock code, it's V4-rewritten code whose rewritten condition depends on the very singleton flags that the trace showed being written by the activation function.

Also worth noting: 0x007b78eb (75 0c) is the V4 "in-game feat tracking gate" inside CalcShouldTrackFeatProgress (raw 0x7b7850 + 0x9b), a different site from V8's 0x007b786b.

Now let me decode the stock tail semantics precisely so I state it correctly:

0x1400af783: 80 7f 61 00     cmp byte ptr [rdi+0x61], 0
0x1400af787: 74 0c           je  0x1400af795      (+0x0c from 0x1400af789)
0x1400af789: 80 7f 63 00     cmp byte ptr [rdi+0x63], 0
0x1400af78d: 74 06           je  0x1400af795      (+6 from 0x1400af78f)
0x1400af78f: 80 7f 62 00     cmp byte ptr [rdi+0x62], 0
0x1400af793: 74 02           je  0x1400af797      (+2 from 0x1400af795)
0x1400af795: 33 f6           xor esi, esi
0x1400af797: 40 0f b6 c6     movzx eax, sil

Wait, let me compute the jump targets carefully.

    74 0c at 0x1400af787 (2 bytes) → next = 0x1400af789; +0x0c = 0x1400af795.
    74 06 at 0x1400af78d → next = 0x1400af78f; +6 = 0x1400af795.
    74 02 at 0x1400af793 → next = 0x1400af795; +2 = 0x1400af797.
    33 f6 (xor esi,esi) at 0x1400af795, 2 bytes → 0x1400af797.
    40 0f b6 c6 (movzx eax, sil) at 0x1400af797.

So: if +0x61==0 → jump to 0x1400af795 (xor esi,esi; movzx eax,sil → eax=0). If +0x63==0 → jump to 0x1400af795 → eax=0. If +0x62==0 → jump to 0x1400af797 (skip the xor) → eax = whatever esi was = nonzero (presumably 1 set earlier). Fall-through (all three nonzero) → xor esi,esi → eax=0.

Hmm, that's odd: fall-through gives 0 and the +0x62==0 case gives nonzero. So stock semantics: eax = (+0x61!=0 && +0x63!=0 && +0x62==0) ? esi : 0. Roughly: requires +0x61 and +0x63 set, and +0x62 clear. That's consistent with "return true iff +0x61 and +0x63 set, and +0x62 (achievement-block) NOT set".

Hmm! So my Part 5 §C2 claim "+0x62 is irrelevant here" is WRONG for stock — stock requires +0x62==0. Let me be careful: 74 02 je → jumps over the xor when +0x62==0, keeping esi (nonzero). When +0x62 != 0, falls through to xor esi,esi → 0. So yes: stock returns nonzero only if +0x62 == 0.

So the correct stock semantics: return (esi) iff [+0x61]!=0 && [+0x63]!=0 && [+0x62]==0, else 0. And V4's rewrite changed it to [+0x61]==1 && ([+0x63]!=0 || [+0x65]!=0), dropping the +0x62 requirement and adding +0x65 as an alternative to +0x63.

That's a meaningful correction to Part 5 §C2. Let me verify V4's patched bytes decode as I think: 31 c0 66 83 7f 61 01 75 0f 80 7f 63 00 75 06 80 7f 65 00 75 03 ff c0 90

    31 c0 xor eax,eax (2) @ +0
    66 83 7f 61 01 cmp word ptr [rdi+0x61], 1 (5) @ +2
    75 0f jne +0x0f @ +7 → next = +9; +0x0f = +0x18 (24) → past the end (24 bytes total, so +0x18 = end) → the ret after
    80 7f 63 00 cmp byte [rdi+0x63], 0 (4) @ +9
    75 06 jne +6 @ +13 → next = +15; +6 = +0x15 (21) → ff c0 inc eax
    80 7f 65 00 cmp byte [rdi+0x65], 0 (4) @ +15
    75 03 jne +3 @ +19 → next = +21; +3 = +24 → end (ret)
    ff c0 inc eax (2) @ +21
    90 nop @ +23

So: eax=0; if [rdi+0x61] (word) != 1 → ret (eax=0). If [+0x63]!=0 → inc eax → eax=1. Else if [+0x65]!=0 → ret with eax=0??

Hmm wait: 75 06 jne → if +0x63 != 0, jump to +21 = ff c0 inc eax → eax=1. Good. If +0x63 == 0, fall to cmp byte [rdi+0x65],0; 75 03 jne → if +0x65 != 0, jump to +24 = end → eax=0. If +0x65 == 0, fall through to ff c0 inc eax → eax=1.

So V4 semantics: eax = ([+0x61]==1) && (([+0x63]!=0) || ([+0x65]==0)). Hmm, that gives 1 when +0x65 is ZERO. That's the inverse of what I'd expect.

Let me redo:

    +0x63 != 0 → jump +6 from +15 → +21 → inc eax → 1
    +0x63 == 0 → continue at +15: cmp [+0x65],0
        +0x65 != 0 → jne taken → jump +3 from +21 → +24 → end, eax=0
        +0x65 == 0 → fall through → +21 inc eax → eax=1

So eax=1 iff +0x61==1 && (+0x63!=0 || +0x65==0). I.e. fails only when +0x63==0 AND +0x65!=0.

Hmm. That matches the patch map's "Effective: save_ok && !ruler_designer && (stock_checksum || !steam_active)" only loosely. Let me not over-interpret the symbolic names.

Given the risk of mis-decoding, the honest approach: state the byte facts (verified) and the structural fact (V4 patched inside the V9-bypassed function), and give the decode with appropriate hedging where the symbolic mapping is uncertain. Actually I decoded it mechanically, which is reliable — the arithmetic is checkable. Let me just present the mechanical decode and note that the symbolic naming in the V4 map is a separate (earlier) interpretation.

Actually, I should double-check my decode by running a disassembler. Is capstone available?

Let me check.

No capstone. Let me try installing it, or use objdump.

Let me check for objdump.

objdump is available. Let me disassemble the stock bytes at raw 0xaeb83 (VA 0x1400af783) and the V4-patched bytes, using objdump on raw binary with the right base address.

Approach: extract the 24 stock bytes and the 24 V4 bytes into files, then objdump -D -b binary -m i386:x86-64 --adjust-vma=0x1400af783.

Actually objdump on a PE directly: objdump -d --start-address=0x1400af783 --stop-address=0x1400af79b CK2game333.exe. Let's try that — it's the real check on the stock bytes.

My hand-decode of the stock tail is confirmed by objdump, byte for byte and jump-target for jump-target.

Stock semantics (now verified by a real disassembler):

    +0x61 == 0 → je 0x1400af795 → xor esi,esi → movzbl sil,eax → eax = 0
    +0x63 == 0 → je 0x1400af795 → eax = 0
    +0x62 == 0 → je 0x1400af797 → skips the xor → eax = esi (nonzero, set earlier in the function)
    all three nonzero → falls through → xor esi,esi → eax = 0

So stock: returns nonzero iff +0x61!=0 && +0x63!=0 && +0x62==0.

That means my Part 5 §C2 statement "+0x62 is irrelevant here" is wrong. Stock requires +0x62 == 0. Since the singleton initialises +0x62 = 0 and the only writer sets it to 1 in the achievement-block path, +0x62==0 normally holds — so the practical effect is usually as I described, but the statement as written is false and must be corrected.

Now let me verify the V4 patched bytes decode too, using objdump on a raw blob.

Let me build a tiny binary blob with the V4 bytes and disassemble at the right VMA.

Also I should double check whether V4's patch is present in the V7/V8/V9 chain images — i.e. that the V9 image really has the V4 tail. I can check the chain images.

Let me do both.

V4 tail decode confirmed:

    xor eax,eax
    cmpw $0x1, 0x61(%rdi); jne 0x1400af79b (past the end → ret with eax=0)
    cmpb $0x0, 0x63(%rdi); jne 0x1400af798 → inc eax → eax=1
    cmpb $0x0, 0x65(%rdi); jne 0x1400af79b → ret with eax=0
    fall through (+0x65==0) → inc eax → eax=1
    nop

So V4 semantics: eax = ([+0x61] == 1) && ([+0x63] != 0 || [+0x65] == 0). Equivalently it returns 0 only when +0x61 != 1, or when +0x63 == 0 && +0x65 != 0.

Note cmpw — a word compare at +0x61, so it reads +0x61 and +0x62 together and requires the pair to equal 1 (i.e. +0x61==1 and +0x62==0). That neatly preserves the stock "+0x62 must be 0" condition! Nice — so V4 kept the +0x62==0 requirement by comparing the word.

So the accurate statement:

    Stock tail: nonzero iff +0x61!=0 && +0x63!=0 && +0x62==0.
    V4 tail (present in V4→V9): 1 iff word[+0x61]==1 (i.e. +0x61==1 && +0x62==0) and (+0x63!=0 or +0x65==0).

And since the V9 image contains the V4 tail, the cold al=0 at CALC_RETURN_PATH means either: (a) the eligibility loop 0x14072D540 returned 0 earlier (before reaching the tail), or (b) the tail's V4 condition failed: word[+0x61]!=1, or (+0x63==0 && +0x65!=0).

But the trace showed CALC_FAIL_PATH (raw 0x7b78f8, which tests [+0x60]==0 && [+0x65]!=0) never fired — meaning NOT(+0x60==0 && +0x65!=0), i.e. +0x60!=0 || +0x65==0. With the singleton default +0x60=0, that implies +0x65==0...

Hold on. Let me re-read the gate description: "singleton 0x1400AF050: [+0x60]==0 and [+0x65]!=0" → CALC_FAIL_PATH. So the fail path is taken when +0x60==0 AND +0x65!=0. Hmm, that's odd (it fails when +0x65 is set?). Whatever the exact polarity, CALC_FAIL_PATH never fired.

If +0x60==0 (default) and the fail path requires +0x65!=0, then not-firing implies +0x65==0. And with +0x65==0, the V4 tail's condition (+0x63!=0 || +0x65==0) is satisfied. So the tail would return 1 provided word[+0x61]==1 (default +0x61=1, +0x62=0 → word = 1 ✓).

⇒ The tail would have returned 1. Therefore the cold al=0 must come from earlier in 0x1400af690 — the 0x14072D540 eligibility-loop path returning 0 (raw 0xAEB7D → 0x1400AF6E1).

That's a genuinely new, sharper conclusion than "the gate returned 0 for unknown reasons", and it's derived from verified bytes + the verified trace. It also strengthens the case for V9 being the right fix (the failing sub-path is the feature-list walk, which is exactly "feature availability not populated yet on cold load").

I should be careful: this chain of inference depends on (a) the V4 tail being present in the V9/V8 image (verifiable), (b) the singleton defaults (from the V9 session's disassembly of 0x1400af050, not independently re-verified by me now), and (c) CALC_FAIL_PATH's polarity as documented. Let me verify (a) directly, and mark (b)/(c) as taken from the V9 session's disassembly.

Let me verify (a): check the V8/V9 chain images have the V4 tail bytes at 0xaeb83.

No output — the chain dict is empty, probably because exec of the module source re-runs but my cap wasn't used (the module-level sha reference inside functions resolves to the module global, which I replaced via B.sha=cap... but exec(..., B.__dict__) redefines sha from the source, overwriting my cap).

Let me instead re-run and capture differently: patch after exec by re-running the patch pipeline. Simpler: replicate using B's own functions after exec.

Let me look at the module's structure — does it have a main()?

There's a main(). Let me import the module properly and monkeypatch sha before calling main().

Note: my earlier harness called B.main() if it existed — it did (hasattr(B,'main')), so it used B.main() with B.sha = capture... but wait, main() calls the module-global sha, which I had replaced with capture. And capture stores into chain. That worked (9 images captured). The problem in the second script was that I used exec(...) which redefined sha. Let me just use B.main() with the monkeypatch.

Definitively confirmed:

    V4 rewrote the eligibility-helper tail at raw 0x000aeb83, and that rewrite is present in V4→V9.
    The V9 site is untouched until V9.

So V9 bypasses a call into a function whose tail V4 had already rewritten. That's a real, previously unrecorded interaction and it sharpens the root cause: with the V4 tail in place and the singleton at defaults, the tail would have returned 1 — so the cold al=0 must originate earlier inside 0x1400af690 (the feature-list eligibility walk at 0x14072D540).

Now let me correct Part 5 §C2 and add this finding. Then continue with the patch map + living docs.

Let me update Part 5 §C2.

Verified with a real disassembler and the project's chain replay. Correcting Part 5 §C2 — one of my claims was wrong:

Let me read the actual §C2 section of Part 5.
Edit+57−11

The text is there. The edit failed probably due to the em-dash or special characters. Let me use a narrower anchor.

Now replace the "Therefore the cold al=0 must come from..." paragraph with the sharper conclusion.

Now update V9_RUNTIME_RESULTS.md §4 with the same correction, and add this to RAWLOG_NETNEW_EXTRACTS §11 (it's a new finding derived during this ingest — actually it's derived, not raw-log-extracted; better placed in the analysis docs + MASTER table. Let me add a §11.8 to RAWLOG_NETNEW_EXTRACTS since that file is "net-new facts").

Actually, this finding is a derived fact from combining two repo docs + verification, not extracted from the raw logs. It belongs in Part 5 (done), V9_RUNTIME_RESULTS (to do), and CONTRADICTIONS (correction of the "+0x62 irrelevant" claim). Let me add it to V9_RUNTIME_RESULTS §4 and mention in CONTRADICTIONS §13 area... Actually it's not a contradiction, it's a refinement. I'll add a short note to V9_RUNTIME_RESULTS §4 and to the patch map.

Let me update V9_RUNTIME_RESULTS §4.
Edit+54−7×2

Now update WINDOWS_333_PATCH_MAP.md: title, current state, add V8/V9 sections, add verdict lines. Let me use edit_file operations.
Edit+36−7

Continue: add V7/V8/V9 sections to the patch map, and update the runtime verdict list.

Let me check the current state of the file around the V6 section and verdict.

Now update the runtime verdict list to add V8/V9, and the CSV. Let me check the CSV format.
Edit+50−8×2

The CSV has the V7 row already. I need to:

    Update the header line to include v8_sha256 and v9_sha256.
    Add V8 and V9 rows.
    Note: the CSV column is patched_bytes_v6 which is a misnomer for later versions, but I'll follow the existing convention (it's the "patched bytes" column).

Also the 0x000aeb83 purpose says "Eligibility helper: stock_checksum || !steam_active" — I'll leave it but could enrich. Better to leave as-is (it's the V4-era interpretation) and let the new docs carry the correction.

Let me append the V8/V9 rows and update the header.

Note: the CSV's last line has no trailing newline? Let me check and append carefully.

Now verify the CSV's stock_bytes column against the actual stock exe for the new rows (and ideally all rows) — a real check.

Let me do that.

All 26 rows of the patch map verify against the real stock exe and the real V9 image, including the VA mapping. That's a strong check.

Now finish the remaining living-doc repairs:

    WINDOWS_333_PATCH_MAP.md runtime verdict lines (add V8/V9)
    MASTER_ARTIFACT_TABLE.md (add V8/V9 + tooling hashes)
    STATUS.md
    CASES_AND_FINDINGS.md
    PLAN.md
    README.md (root of ALL-MY-LOGS-SO-FAR)
    ORGANIZATION_HISTORY.md
    12_raw_chat_logs/INDEX.md
    02_handoffs/NEXT_SESSION_HANDOFF_2026-08-30.md banner

Let me do the verdict lines first, then MASTER_ARTIFACT_TABLE.

Let me batch these efficiently. First the verdict list.

Now MASTER_ARTIFACT_TABLE.md. Let me look at it.
Edit+3−1

Now I need to add V8/V9 rows to section 2, and add a tooling-hash section. Let me get the actual current SHA-256 hashes of the V8/V9 tooling files from disk (rather than trusting session memory), since that's what the table needs.

All hashes match session memory. Note MJ_V8_CLEAN_TRACE.txt is 1665 bytes, but the last log/ copy I earlier recorded as 20,273 B... wait, session memory said MJ_V8_CLEAN_TRACE.txt SHA 49c4a602 — matches. The 20,273 B figure in my dissection report was wrong. Let me re-verify the last log/ file sizes since I quoted them in the dissection report.

Let me check.

Corrections needed in my dissection report:

    MJ_V8_CLEAN_TRACE.txt is 1,665 bytes, not 20,273 B (I wrote 20,273 — wrong).
    RUN_MJ_V8_CLEAN_TRACE.bat is 3,088 bytes, not 4,009 B (wrong).
    The patch is 28,583 bytes, not 77,855 B (wrong).
    Total folder size is 1,716,243 bytes, not 1,893,504 (wrong).

Line counts are all correct. Let me fix the dissection report.

Note: MJ_V8_CLEAN_TRACE.txt is only 48 lines / 1665 bytes — but earlier I read it as a full trace script. Fine.

Let me fix these numbers.

Found errors in my own dissection report — the byte sizes were wrong. Fixing against stat:

Also fix the patch filename in the dissection report §1 table (I wrote the long name 01a044b2-…f72e11e0….patch but the actual file is 01a044b2-855a-71fb-8d40-d584a0ce8e2a.patch). I just fixed row 9. Let me also check whether 11_git_patch/ has the same filename.

And fix "1,893,504 bytes" if it appears elsewhere. Let me grep.

Fix Part 5's byte total too. And confirm the patch in 11_git_patch/ is byte-identical (cmp).

All three duplicates re-verified byte-identical with cmp.

Now: MASTER_ARTIFACT_TABLE.md — add V8/V9 rows + a tooling section. Let me do that.

Now add a tooling section to MASTER_ARTIFACT_TABLE.md. Let me find a good insertion point — after section 2 or as a new section. Let me see the section list.
Edit+3−1

Section 6 is "Patcher toolchain (V2–V7...)". Let me look at it and extend to V8/V9.

Also section 5 "Local feat-progress cache" — I should update with the new user_ids.

Let me view sections 5 and 6.

Now extend section 5 and 6. Let me do both edits.

Now extend section 6 with V8/V9 tooling.

Now append the V8/V9 tooling rows to section 6 of MASTER_ARTIFACT_TABLE.md.

Note: RUN_APPLY_CK2_MJ_V9_INLINE.ps1 hash 02ce982e… is the hash of the reconstructed file (with my provenance header), so I should label it as such.

Also test guides: add V8/V9 guide hashes. Let me get them.
Edit+21−4×2

Now append the V8/V9 tooling rows to MASTER_ARTIFACT_TABLE section 6.

Now add the test-guide hashes for V8/V9 to the "Test guides:" paragraph.

Then do STATUS.md, CASES_AND_FINDINGS.md, PLAN.md, README.md, ORGANIZATION_HISTORY.md, 12_raw_chat_logs/INDEX.md, handoff banner. And finally delete the 3 duplicate files from last log/.

Let me do the test guides line.

Continue: STATUS.md, CASES_AND_FINDINGS.md, PLAN.md, README.md, ORGANIZATION_HISTORY.md, 12_raw_chat_logs/INDEX.md, handoff banner, and the teardown of the 3 duplicate files.

Let me look at STATUS.md.
Edit+33−2×2

I'll prepend a new banner to STATUS.md for 2026-08-30 and update the "Bottom line" section.

Let me do the banner first.

Continue: update the "Last updated" line and "Bottom line" section of STATUS.md.

Continue: the "Bottom line (V7 verdict)" section in STATUS.md.

Now update the "Proven working" and "Still open" lists in STATUS.md.

Let me look at what's there now.
Edit+60−16×3

Continue with the STATUS.md "Proven working" / "Still open" edits. The cold-load bullet must move from "Still open" to "Proven working", and the identity-drift claim must be corrected.

Let me do it.

Continue: replace the "Still open" cold-load bullet with a closed entry.

Continue. Next: CASES_AND_FINDINGS.md, PLAN.md, README.md, ORGANIZATION_HISTORY.md, 12_raw_chat_logs/INDEX.md, handoff banner, then teardown of the 3 duplicates.

Let me look at CASES_AND_FINDINGS.md.
Edit+44−28×2

I need to find the C19/C20... rows for the feat reset case. Let me grep for the feat-reset case ID.

There's no dedicated case for the cold-load feat reset. I should add one — call it C26 (C25 is the launcher). Let me check line 90-130 for context, then add C26 to the case map and a finding.

Let me view lines 90-135.

I need to finish the CASES_AND_FINDINGS.md updates: add C26 to the case map, add F11, and update F9. Let me do that now.

First, add C26 row to the case map (after C25 or at the end of the C-rows).

Continuing: add F11 to CASES_AND_FINDINGS.md and update F9, then PLAN.md, README.md, ORGANIZATION_HISTORY.md, 12_raw_chat_logs/INDEX.md, handoff banner, teardown.

Let me update F9 and add F11.

Continue: update the header line ("Cases C01–C24") and the "Do not re-open" line in CASES_AND_FINDINGS.md.
Edit+38−5×2

Continuing with PLAN.md, README.md, ORGANIZATION_HISTORY.md, 12_raw_chat_logs/INDEX.md, handoff banner, then teardown.

Let me look at PLAN.md and README.md.

Continuing. Let me update PLAN.md's main goal line and README.md.

Let me find the relevant PLAN.md phase for feats.

Continuing. Let me look at PLAN.md phases 2-4 to add the V8/V9 work.

Continue: insert a Phase 2.5 (V8/V9) into PLAN.md and update the header line.

Continuing. Update the PLAN.md main goal line, then README.md, ORGANIZATION_HISTORY.md, 12_raw_chat_logs/INDEX.md, handoff banner, teardown.
Edit+24−3

Continuing. Now README.md of ALL-MY-LOGS-SO-FAR.

Continuing. Now: ORGANIZATION_HISTORY.md, 12_raw_chat_logs/INDEX.md, handoff banner, then teardown of the 3 duplicate files.

Let me look at ORGANIZATION_HISTORY.md tail and 12_raw_chat_logs/INDEX.md.

I need to continue. Remaining work:

    ORGANIZATION_HISTORY.md — add Turn 9 entry
    12_raw_chat_logs/INDEX.md — add the last log/ ledger
    02_handoffs/NEXT_SESSION_HANDOFF_2026-08-30.md — banner
    Teardown: delete the 3 duplicate files from last log/
    Final verification pass

Let me do ORGANIZATION_HISTORY.md and INDEX.md.

Continuing. Now the INDEX.md ledger for last log/. Let me append a section.

Continuing. Remaining:

    02_handoffs/NEXT_SESSION_HANDOFF_2026-08-30.md banner
    Teardown: delete the 3 duplicate files from last log/
    Final verification pass

Let me do the handoff banner.

Continuing: the handoff banner says "V8 baseline, V9 needs one cold rerun to confirm" — that's now stale. Let me prepend an updated banner.

Continuing. Now the teardown: delete the 3 byte-identical duplicates from last log/. I already verified with cmp that they're identical.

Let me do that.
Edit+28−3

Now the teardown — the three byte-identical duplicates (re-verified with cmp earlier this turn):

The three duplicates are removed; 8 raw exports remain, all ledgered.

Now let me update the dissection report's §6 to reflect that the teardown actually happened (it says "were removed" — let me verify the wording matches). Actually in §1 of the dissection report I wrote "BYTE-IDENTICAL DUPLICATE of ... — the archived copy is canonical" without saying removed; §6 says "were removed from last log/". Good, that's now accurate.

But §1's table header says 11 files — still accurate as the inventory at the time. Let me add a note that the 3 were subsequently removed. Actually §6 covers it. Let me add a short line to §1 for clarity.

Now the final verification pass. Let me:

    Verify all new/edited files exist and are non-empty.
    Check for any remaining stale claims (e.g., "V7 = current baseline", "v6 is operative") across the tree.
    Re-run build_v9_chain.py one more time as the project's own check.
    Verify markdown link targets referenced in new docs actually exist.

Let me do a comprehensive check.

Continuing the final verification pass. Let me check for stale claims across the tree and verify cross-references.

Wait — one thing I notice: 03_analysis/CONTRADICTIONS.md is 10,793 bytes but earlier (before my §12-§14 append) it was 7,002. Good. And RAWLOG_NETNEW_EXTRACTS.md shows 17,658 — but that was the size AFTER my §11 edit earlier... let me re-check: earlier ls showed 17,658 at 18:26 which was after the first §11 append. Then I edited §11.6. Let me confirm the §11.6 edit is present.

Let me do the checks.

Continuing the final verification pass. Let me check for stale claims across the tree, then re-run the project's own check.

Let me search for stale statements that contradict the new state.

The only remaining "baseline is V8" is inside the handoff's superseded banner, which I explicitly labeled as superseded. That's intentional.

Let me check the other handoff (2026-08-29) and CK2_MJ_ULTIMATE_HANDOFF.md for stale baseline claims, plus check that V8_TEST_GUIDE gets a warning banner (I referenced it in MASTER table as needing a caveat — better to add the banner to the file itself).

Let me do those.

Continuing. I found remaining stale claims to fix:

    02_handoffs/CK2_MJ_ULTIMATE_HANDOFF.md:140 — "V7 Continue (current in-game baseline)"
    02_handoffs/NEXT_SESSION_HANDOFF_2026-08-29.md:50 — "V7 (current) … what the user runs now"
    00_START_HERE/STATUS.md:188 — the patch table inside STATUS.md still says V7 = current in-game Continue
    INGEST_PLAN_DUMP_2026-08-27.md — historical planning doc; leave as history (it's a dated plan, not a living doc).
    PROMPT_organize_research_log_v5.md / _v6.md — history, superseded by v7. Leave, but v7 already declares itself operative._

Let me fix 1, 2, 3. Let me look at the STATUS.md:188 context first.

Continuing: fix the three stale baseline tables.

    STATUS.md naming-collision table: add V8/V9 rows.
    CK2_MJ_ULTIMATE_HANDOFF.md: update the V7 row + add V8/V9.
    NEXT_SESSION_HANDOFF_2026-08-29.md: it's a dated handoff superseded by the 2026-08-30 one; add a banner rather than rewriting its table.

Let me do these.

Continuing: fix the two handoff docs.
Edit+3−1

Continuing: add a superseded banner to NEXT_SESSION_HANDOFF_2026-08-29.md, then add a warning banner to CK2_MJ_V8_TEST_GUIDE.md, then run the final verification.
Edit+10−2

Continuing: add the superseded banner to the 2026-08-29 handoff, then the V8 test guide banner, then final verification.

Continuing: add the warning banner to CK2_MJ_V8_TEST_GUIDE.md, then run final verification.
Edit+20−3

Let me continue with the final verification pass. I need to:

    Re-run the project's own check (build_v9_chain.py) — the real verification.
    Verify cross-references in new docs point to files that exist.
    Confirm the V8 test guide hash changed (I referenced a hash in MASTER_ARTIFACT_TABLE — need to update it since I just edited the file!).

That's important: I edited CK2_MJ_V8_TEST_GUIDE.md after recording its hash 3ea0a08a… in MASTER_ARTIFACT_TABLE.md. Need to re-hash and update.

Let me do all of that.
Edit+22−3

The V8 guide edit invalidated a hash I'd just recorded. Re-hashing and fixing:

Continuing: update the V8 guide hash in MASTER_ARTIFACT_TABLE.md.

Continuing the final verification pass. Let me:

    Re-run the project's own check (build_v9_chain.py).
    Verify every cross-referenced path in the new docs actually exists.
    Verify the tooling hashes recorded in MASTER_ARTIFACT_TABLE still match disk (since I want to be sure nothing drifted).

Let me write one comprehensive check.
Edit+3−3

The check came back: 37 files' on-disk hashes appear in the table, and the 9 "missing" ones are analysis markdown docs and cache captures — the table never claimed to hash those (they're not patcher tooling). That's expected, not a defect.

Let me do the final cross-reference check: verify every path referenced in my new docs actually exists.

Then finish. Let me run that check.

Two classes of "missing" came back. Let me check each:

    The six .ck2 saves in Part 5 §C4 — those are the user's saves that were never uploaded (they're in the save inventory table from the raw log). That's expected and correct — the table explicitly says "user-side, not uploaded". Not a defect, but let me verify the claim that they're not in the repo.

    INGEST_2026-08-27.md / INGEST_2026-08-29.md referenced in the dissection report — these ARE wrong filenames. The actual files are INGEST_PLAN_DUMP_2026-08-27.md and INGEST_REPORT_DUMP_2026-08-27.md. Let me check what ingest files actually exist and fix the reference.

Let me check both.

Two flags came back. Checking each rather than assuming:

Let me finish the two flagged items:

A. The six .ck2 files in Part 5 §C4 are the user's saves that were never uploaded — the repo has 5 different saves. My Part 5 §C4 table already says "user-side, not uploaded". Let me verify that note is present and clear, since the checker flagged them.

B. INGEST_2026-08-27.md / INGEST_2026-08-29.md in the dissection report are wrong filenames. The real ones are INGEST_PLAN_DUMP_2026-08-27.md and INGEST_REPORT_DUMP_2026-08-27.md. There is no 2026-08-29 ingest doc. Let me fix that reference.

Let me do both.

The Part 5 table already says "user-side, not uploaded" — that's correct and the checker flag was a false positive. But I should make it unmistakable, since a future reader might hunt for those files. Let me strengthen the note and fix the wrong INGEST filenames.

Let me do both fixes.

Let me finish up. I should re-run the cross-reference check to confirm the fixes, then do a final overall verification and report to the user.

Let me re-run the checks.

All the remaining "missing" entries are pre-existing content in files I did not create — they're the deleted raw log filenames that 12_raw_chat_logs/INDEX.md and RAWLOG_NETNEW_EXTRACTS.md intentionally reference as historical records (those files were deleted in the 2026-08-26 pass by design, per the INDEX's own header). Plus settings.txt (a CK2 game config file, not a repo file) and DISSECTION_REPORT.md (the older report, since renamed to dated versions).

So these are all expected, not defects from my work. Let me confirm that quickly, then do the final verification and report.

Let me verify: those filenames appear in the pre-existing sections of INDEX.md, not my new section. Let me check whether any of them appear in MY added section.

All remaining flags are in pre-existing text, not my additions. Confirming that rather than assuming:

Let me do a final wrap-up verification and then report to the user. Let me check git status to summarize what changed, and do one final run of the project's own check.

I see there's a __pycache__ directory that got created by my running build_v9_chain.py. That's a generated artifact that shouldn't be committed. Let me check if there's a .gitignore and clean it up.

Then I should give the final report to the user.

Let me clean up the pycache and check .gitignore.

One thing to clean up before I report — my verification runs created a __pycache__:

There's no .gitignore in the repo. My verification run created ALL-MY-LOGS-SO-FAR/05_patches_and_scripts/py/__pycache__/. That's a generated artifact from my own import of build_v9_chain — it shouldn't be left in the repo. Let me remove it.

Then give the final report.

Let me review where I am. I've completed all the work items. Let me do a final check that everything is complete and then give the user a comprehensive report.

Let me verify the state of the workspace one final time and then report.

Actually, let me re-read the task list from session memory:

    ✅ 01_research_archives/CK2_MJ_RESEARCH_ARCHIVE_PART5.md — written
    ✅ 00_START_HERE/DISSECTION_REPORT_2026-08-30.md — written
    ✅ §11 appended to 03_analysis/RAWLOG_NETNEW_EXTRACTS.md
    ✅ 03_analysis/V9_RUNTIME_RESULTS.md and 03_analysis/FEAT_CACHE_PEAK_TIER_ICON.md
    ✅ 04_test_guides_and_reports/CK2_MJ_V9_TEST_GUIDE.md
    ✅ 05_patches_and_scripts/ps1/RUN_APPLY_CK2_MJ_V9_INLINE.ps1 reconstructed
    ✅ Living-doc repairs: STATUS.md, CASES_AND_FINDINGS.md, MASTER_ARTIFACT_TABLE.md, PLAN.md, README.md, CONTRADICTIONS.md, 12_raw_chat_logs/INDEX.md, ORGANIZATION_HISTORY.md, handoff banner
    ✅ Raw-file teardown: 3 duplicates deleted, 8 raw exports kept with ledger entries

Plus the user's two questions:

    V9 result: reported
    Bronze icon / cache question: answered

Everything is done. Let me do a final sanity check and then present the deliverables.

Let me present the main files to the user. The key deliverables are:

    PROMPT_organize_research_log_v7.md (the consolidated rulebook — deliverable 1)
    DISSECTION_REPORT_2026-08-30.md (the dissection)
    FEAT_CACHE_PEAK_TIER_ICON.md (the answer to their question)
    V9_RUNTIME_RESULTS.md

Let me present the most relevant one. Per the tool description, present the main deliverable and mention others by name. The user's core ask was: consolidate the instructions, then dissect. And report the V9 result + answer the cache question.

Let me present the dissection report as the main artifact since it's the map of everything, or maybe the v7 prompt since that was step 1. Actually the user's most emotionally-engaged question is the Bronze icon one. But the primary deliverable is the organization work.

Let me present the DISSECTION_REPORT since it's the index of everything, and mention the others.

Actually, let me reconsider — the user asked for (1) consolidated instructions, (2) dissection into folders, (3) V9 result + cache answer. The v7 prompt is deliverable 1. Let me present that one since it's the thing they explicitly asked to be created "as one continuous thing."

Hmm, but they may want to see the answer to their question most. Let me present the dissection report which links everything, and clearly summarize the V9 + cache answers in my message.

Let me do a final verification first.

Everything is complete. Let me present the main deliverable and give a final report.

Let me present the dissection report as the main artifact since it's the index of the whole operation.

Actually — the user's request had two halves: (1) consolidate the instructions into one continuous thing, then dissect; (2) report the V9 result and answer the Bronze-icon question. The v7 prompt is deliverable 1. Let me present that one, since it's the thing they explicitly asked to be created.

Hmm, but the dissection report is the map of everything. Let me present the v7 prompt (step 1, the explicit ask) and mention the rest by name. Actually, I think the most useful single file to open is the DISSECTION_REPORT since it indexes where everything went. But the user asked for the consolidated rulebook as the first deliverable.

Let me present PROMPT_organize_research_log_v7.md — it's the "one continuous thing" they asked for.

Then write the final report covering:

    What I did (both steps)
    The V9 result
    The Bronze icon answer (their actual question)
    What I verified
    What was wrong in my earlier work / corrections issued

Let me be concise but complete.

Everything is done. Let me write the final report to the user.

Let me summarize:

    Both steps of the request completed
    The V9 result
    The Bronze icon answer (their actual question)
    What I verified
    Corrections I issued

Let me be clear and organized.

Both halves of your request are done, plus the answer to your Bronze-medal question.
1. Collected into one continuous thing

00_START_HERE/PROMPT_organize_research_log_v7.md (332 lines) merges v4's §1–§10 skeleton and 12 rules, v5's §4–§7 (artifact→folder map, SHA-256 dedup, living docs, integrity), and v6's §8–§13 (dashboard, naming collisions, Continue surfaces, tooling integrity) into a single operative rulebook. It declares itself as the text to paste; v4–v6 stay as history.
2. Dissected into the folders

last log/ held six distinct sessions, not one — plus PRs #13/#14/#15. Full disposition in 00_START_HERE/DISSECTION_REPORT_2026-08-30.md.

Created: Part 5 archive (V8 disproof → clean trace → V9 fix, 601 lines), V9_RUNTIME_RESULTS.md, FEAT_CACHE_PEAK_TIER_ICON.md, CK2_MJ_V9_TEST_GUIDE.md, and RAWLOG_NETNEW_EXTRACTS.md §11. Repaired: STATUS, CASES_AND_FINDINGS (C26/C27, F11), MASTER_ARTIFACT_TABLE, patch map + CSV, PLAN, README, ORGANIZATION_HISTORY, the chat INDEX, and superseded banners on both old handoffs.

Three byte-identical duplicates were deleted from last log/ (cmp-verified against their canonical copies first). The 8 raw exports are kept with ledger entries.
3. The V9 result

It works — that's confirmed, and V9 is now the baseline (61e4345b…). V8 was applied and its premise was disproven: the trace logged RESTORE_GATE al=1 on the cold burst, meaning IsActiveForPlaythrough returned true anyway. The real failure was the final gate at raw 0x007B7906.
4. Your Bronze question — you're right

Your hypothesis is correct, and it's not a bug. Proof: your day-one save carries global_established=2.000, but Bronze needs 4 — so the medal can only be coming from the cache. The cache stores your lifetime peak per Windows profile, shared by every save and campaign. The "Word has spread…" popup is a level-grant event, so it fires only for a new tier. FEAT_HIGHSCORE_TT ("Best Result") reads the cache; FEAT_CURRENT_TT ("Current Progress") reads the save. Reset is optional — back up and rename the cache file. Never wipe_feats.
What I verified

build_v9_chain.py re-ran clean, reproducing every hash V2→V9. objdump decoded both variants of the eligibility-helper tail — which surfaced that V4 already rewrote the tail inside the very function V9 bypasses, sharpening the root cause. All 26 patch-map rows check against the real stock exe and V9 image (0 mismatches). I parsed the reconstructed PS1 from disk and simulated it against real V7 and V8 images — both produce the exact V9 hash.
Four things I got wrong and fixed

    My first draft said +0x62 was irrelevant in the gate — false; stock requires it clear.
    My dissection report had wrong byte sizes for three files and the folder total.
    I'd recorded a hash for the V8 guide, then edited that guide — re-hashed (3ea0a08a… → 7cd3916c…).
    I cited two INGEST filenames that don't exist.

Three corrections against pre-existing docs are logged in CONTRADICTIONS.md §12–§14 — including that "identity drift is refuted" is withdrawn (four distinct user_id values are on record).

good. so now check if everything is good. then do "Create PR" and them merge

The user wants me to:

    Check if everything is good
    Create a PR
    Merge it

Let me first check everything is good — run verification checks. Then commit, push to the session branch, create a PR, and merge it.

Important: I'm on branch arena/01a053d9-ck2-monarch-s-journey-restorat. I must commit to it and push only to it, and open a PR from it.

Let me start with a thorough check.

Let me first run the verification checks again:

    build_v9_chain.py
    CSV verification
    Cross-reference check
    Check for any leftover issues

Then commit, push, PR, merge.

Let me do the checks first.

Let me run the full check suite before committing.

Continue with the remaining checks, then commit, push, PR, merge.

Let me run the CSV verification and cross-reference check.

Let me continue: run the remaining checks (cross-references, no stale claims), clean up pycache, then commit, push, create PR, merge.

All checks pass. Now I need to commit, push, create PR, and merge.

Let me commit the work first.

All eight checks pass. Committing now.

26 files staged. Let me commit.

Committed as 92213d8. Now push to the session branch and create the PR.

Let me push first.

Pushed. Now create the PR with gh.

Let me check gh auth status first and then create the PR.

Note: the repo is shallow. That might affect gh pr create — it needs a base branch. The base is main. Let me try.

Let me check the default branch and gh auth.

Continue: create the PR. Base is main, head is the session branch.

Let me create the PR with a good body.

PR #17 created. Now I need to check its status and merge it.

Let me check the PR status first.

PR #17 created. Checking its state before merging:

The PR is MERGEABLE with mergeStateStatus CLEAN. Now merge it.

Which merge method? The repo's history: PRs #13/#14 used merge commits, PR #15 was squash. Either is fine. Given this is one coherent commit with a detailed message, I'll use --merge (merge commit) to preserve the full commit message — actually, with a single commit, squash would also preserve it. The repo has used both. Let me use --merge to preserve the commit as-is since I wrote a detailed multi-paragraph message.

Actually, let me reconsider: squash would collapse my one commit into one — same result. Merge commit keeps the branch topology. Given PRs #13/#14 used merge commits and #15 used squash, both are established. I'll use --merge (merge commit) since that preserves the detailed commit body verbatim.

Hmm, actually let me think about what's cleanest for the user's repo. The user re-uploads via web UI between sessions, and the local clone is shallow. A merge commit is fine and matches the more recent common pattern (#13/#14). Let me use --merge.

Also should I delete the branch after? The system prompt says never switch to/create/push to any other branch, and work is tracked by this branch. Deleting the remote branch could be risky. Let me NOT delete the branch — keep it. Actually gh pr merge --delete-branch would delete it. I should avoid that since the session is tracked by this branch.

Let me merge without deleting the branch.

Mergeable and clean. Merging now (merge commit, preserving the full commit message — matching PRs #13/#14 in this repo's history):

The merge is done. Let me verify the final state — confirm main now contains the work, and that the branch is intact.

Let me do a final verification of the merged state.

Merged. Verifying the result landed on main:

The work is merged. Let me give a final summary. Everything is verified. Let me report clearly.

Let me double-check one thing — the working tree state and that the session branch is still intact.

Everything checked out and the work is merged.
Checks run (all passed)
Check	Result
build_v9_chain.py — the project's own tool	Clean; reproduced every hash V2→V9 including v9 61e4345b…
Patch map CSV vs real binaries	26 rows, 0 mismatches (stock bytes, VA mapping, patched bytes)
Reconstructed PS1 simulated on real V7 and V8 images	Both produce the declared V9 hash
Cross-references in new/edited docs	All resolve on disk
Stale "current baseline" claims	None outside superseded banners
Generated artifacts (__pycache__)	Cleaned; 0 in the commit
PR #17 — merged

https://github.com/UselessMoron123/CK2-Monarch-s-Journey-Restoration/pull/17

    Merge commit 1fd74b5, content commit 92213d8
    26 files: 7 added, 16 modified, 3 deleted (+2,426 / −773)
    mergeStateStatus: CLEAN before merge; state=MERGED at 19:01:02Z

Verified on origin/main after the merge: all 7 new files present, and the 3 byte-identical duplicates confirmed removed.

I used a merge commit rather than squash to keep the detailed commit body intact — that matches PRs #13/#14 in this repo's history (#15 was squash). I did not delete the branch, since this session is tracked by arena/01a053d9-ck2-monarch-s-journey-restorat.

One thing worth flagging for next time: MJ_V9_CLEAN_TRACE.txt still carries the +666146 → +667146 typo. I left it deliberately because its SHA-256 is published in README_MJ_V9_CLEAN_TRACE.md — fixing the file means re-publishing that hash in the same change, never silently. It's tracked as CONTRADICTIONS.md §13 and as an open checkbox in PLAN.md Phase 2b.