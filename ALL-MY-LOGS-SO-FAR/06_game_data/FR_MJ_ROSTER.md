# Pointer

The full Featured Rulers + Monarch’s Journey roster (all bios, all
Bronze/Silver/Gold challenges, payload coverage matrix) lives at:

**`../00_START_HERE/FR_MJ_COMPLETE_ROSTER.md`**

This folder still has the raw wiki paste as `descriptions and challenges.txt`
and the live JSON as `monarchs` / `monarchs.txt` / `monarchs_reactivated.txt`.
