# Prompt for another AI — organizing a part of the CK2 Monarch's Journey research log (v2)

**How to use:** paste everything below the line (together with, or right before, the raw log file of that part) into the other AI. If that part covers a specific patch version or session, say so in one sentence at the top.

> **What changed vs v1, and why (delete this block when pasting):**
> v1 was already good; these additions come from actually running it on Part 2 (the Windows-analysis / failed-V6 log). Each one closes a real gap found during that job:
> 1. **A0 "read this first" block** — future sessions need the safe state + banned artifacts *before* the story, not buried in section F.
> 2. **Error ledger** — the log's most valuable content was the assistant's own wrong claims and their corrections (a semantically wrong but byte-perfect patch, a misread screenshot date, a mis-remembered patch length). v1 only had "method lessons"; now recording errors + corrections verbatim is mandatory.
> 3. **Banned-artifact register** — failed builds (hashes) and unsafe functions must be explicitly blacklisted, or a future session will rebuild them.
> 4. **Deployment logs = ground truth** — apply/patcher logs contain the definitive cumulative offset map and can fill gaps narrative text left (v1's archive lacked v5's offsets; the apply log in the next part supplied them).
> 5. **Recompute rule extended** — beyond dates/sizes: jump/branch displacements, instruction encodings, VA↔file-offset conversions. This caught (and cleared) the failed patch's mechanics.
> 6. **Multi-session logs** — a single export can wrap around or overlap other chats ("father" sessions); true chronology must be reconstructed and interleaving guidance given for merging.
> 7. **User runtime reports verbatim** — error-log lines and crash triggers are primary evidence, not noise.
> 8. **Contradictions protocol** — list both claims, prefer primary evidence, never silently pick a side (a session claimed "version 3.3.5.1 doesn't exist" against an earlier string dump proving it does).
> 9. **Loose-ends register** — one-mentioned-never-followed-up observations (e.g. "unexpected 77 gold") need a home or they vanish.
> 10. **Session-created documents + end-of-log workspace inventory** — reports written during the session (with line counts) and the final file list are evidence pointers the user may hold in other chats.
> 11. **"Materials received but NOT yet analyzed" must be flagged** — the single most important handoff fact can be "the fix's raw material is already uploaded and verified, untouched."
> 12. **Handoff brief verbatim** — if the log contains a prepared one-liner/summary for the next session, quote it exactly.

---

You are organizing a raw exported conversation log — a part of a larger reverse-engineering research project. The project: **restoring the retired "Monarch's Journey" mode of Crusader Kings II to run fully offline on Windows** (via payload re-activation, version archaeology, binary analysis, and hash-guarded executable patches; patch generations are named v1…v6, later parts may go higher — some generations FAILED and are banned). The logs are chats between a non-technical Windows user and an AI assistant, including file-upload listings, tool outputs, screenshot references, and AI-written reports. A single export may contain one chat that spans/overlaps other chats (the user works in several sessions and calls earlier ones e.g. "your father"); reconstruct the true chronology.

Your job: turn the raw log into ONE structured Markdown **research archive** file so that no important information is ever lost. Do not summarize away detail — compress only true noise. Future sessions (human or AI) will rely on this file exclusively, without access to the raw log.

**Produce exactly this structure** (same skeleton as the other parts, so files can be merged):

- **A. ORIENTATION** — **A0 "read this first"**: current safe state, banned artifacts (failed build hashes, functions that must never be called again), and the immediate next action; A1 goal in one sentence; A2 status dashboard (works / broken / open) *as of the end of this part*; A3 where this part sits in time relative to other parts/sessions (note any overlap or wrap-around); A4 one-paragraph story of this part.
- **B. STORY** — B1 timeline of this part (numbered events: action → observation); B2 evolution of the mental model (each working theory in turn and what confirmed or killed it — wrong theories are valuable, record them); B3 if new patch generations/tools appear: lineage table (what changed, runtime result, what it proved) — **failed generations get full entries, with the exact bytes kept "for the record — do not reuse"**.
- **C. KNOWLEDGE BASE** — architecture/mechanics learned; version & content facts; **all verified calculations** (recompute every timestamp, offset arithmetic, size delta, hash length, **jump/branch displacement, instruction encoding, and VA↔file-offset conversion** you find — report any discrepancy with the log); complete artifact table (file, size, SHA-256, path — mark which values are carried over from earlier parts vs. newly stated); **complete patch-offset map — extract the definitive cumulative table from any deployment/apply/patcher log, since those are ground truth and may fill gaps the narrative left**; key code addresses/functions (including helpers whose *data direction* was established).
- **D. ATTEMPTS & DEAD ENDS** — ledger of **directions taken, each with a verdict (✅/❌/🟡) and what it proved**; dead ends closed WITH the evidence that closed them; disproven theories; **an explicit "do not revisit / banned" list** (failed build hashes, unsafe functions, superseded approaches).
- **E. OPEN THREADS & FUTURE DIRECTIONS** — open questions ranked (including **anomalies mentioned once and never resolved** — they get numbered entries too); concrete next steps; deferred/unexplored directions the log mentions or implies; ideas beyond the original goal (adjacent opportunities, reusable techniques, maintenance deadlines like expiring timestamps); **flag any materials that were received/verified but NOT yet analyzed**; **the exact handoff for the next session, quoting any prepared brief verbatim**.
- **F. CONTEXT** — environment & constraints (OS, paths, upload limits, offline requirement, workspace budget, chat/message limits, user skill level); safety rules (e.g. never run `wipe_feats`, never share `pdx_login.txt`/tokens, never distribute patched executables — only guarded patchers; **plus this part's banned builds/functions**); curiosities/side-findings not needed for the goal (hidden console commands, dev codenames, embedded source filenames, stock-game quirks, error-log fingerprints); method lessons — **including an error ledger: every place the log's AI (or user) was wrong, what the wrong claim was, what the correction was, and how it was caught**; evidence inventory (which uploads/screenshots/logs prove which conclusion, screenshot numbers mapped to milestones, **documents written during the session with their line counts, and the end-of-log workspace file list**).

**Rules:**
1. Preserve verbatim: all SHA-256 hashes, file sizes, file offsets and byte patterns, timestamps/unix values, version/build strings, function addresses, exact file paths, console commands, parser keys, save-token IDs, error-log lines, and **the user's runtime reports (crash triggers, UI states, log excerpts) — these are primary evidence**.
2. Every numeric claim from the log must be independently recomputed where possible (dates from unix times, overflow arithmetic, deltas, branch displacements, section-offset conversions). Flag mismatches explicitly. A patch that is byte-perfect but semantically wrong is a key finding — record mechanics and semantics separately.
3. Never invent facts. If something is ambiguous or unresolved at the part's end, record it as an open question — an unresolved ending is a finding, not a failure.
4. Noise to remove: upload receipts and "part N arrived" confirmations, greetings/thanks, repeated file lists, full script/batch sources (record only purpose + identifying hash), duplicate explanations, session-housekeeping chatter — but keep the *facts* those contained. Streaming "thinking out loud" fragments should be collapsed into the conclusions they reached, keeping every wrong turn that changed the direction.
5. **Contradictions protocol:** where this part conflicts with earlier parts (or with itself at different moments), list both claims with their evidence pointers, state which evidence is primary, and never silently pick a side — put it under the final "Merge notes" heading together with what is new vs. restated.
6. If the log ends mid-problem, the last section must state exactly where it stopped, what materials are already in hand, and the immediate next action.
7. Where a session's own recommendation or prepared brief for the next session exists, quote it verbatim rather than paraphrasing.

Output: a single Markdown file titled `RESEARCH ARCHIVE — CK2 Monarch's Journey — Part <n> (<short description>)`.
